# DigiCommunication Nyo - Datou! Black Gemagema Dan (GBA) 한글 패치

갱신: 2026-08-31 — **최신 메시지/카드/종료버튼/미니게임/이름 입력 + 명함 보기 하단 버튼 3종 + 미니게임 캐릭터명 8x8 받침/누락 글리프 수정까지 최종 통합.**

- 1~14차 패스의 진행 기록: `STATUS_old_passes.md` (**포맷 관련 결론 상당수가 틀림**)
- 번역 착수 직전 시점의 기록: `STATUS_pre_translation.md`

## 원본

- ROM: `D:\game\gba\DigiCommunication Nyo - Datou! Black Gemagema Dan (Japan)\...gba`
- SHA-256: `3a098b5963daf8bf38d67780f95325158fe09783980b1b74fa24ba02ecd16a5c`
- 16MB, 게임코드 `BDKJ`. Di Gi Charat(데지코) IP 기반 미니게임집. 개발 M2.
- BIOS(사용자 제공)는 `%LOCALAPPDATA%\emucap\firmware\gba_bios.bin`

## 현재 상태

| 항목 | 상태 |
|---|---|
| 텍스트 추출 | 완료 — 313레코드 / 4,032줄 |
| 이미지 추출 | 완료 — 1,257장 / 일본어 비자막 이미지 113장 선별 |
| 자막·가사 이미지 추출·재작화 | 완료 — 350장 (**ROM 재삽입 미착수**) |
| 폰트 한글 이식 | 완료 — 2,350자 |
| 글상자 실측 | 완료 |
| 용어집 | 완료 — 46항목 |
| 대사 번역 | **3,996 / 3,996줄 (100%)** |
| `ui_strings.json` 번역 | **미착수 (0 / 393)** |
| 자막 이미지 번역 | **완료 (350 / 350)** — `PARTY☆NIGHT` 가사 044~085 포함 |
| 재삽입·빌드 | 완료 — 비자막 번역 이미지 111장 포함 `build/digicomm_nyo_kr_name16.gba` |
| **실기 검증** | **부분 완료 / UI 결함 발견** — 깨끗한 부팅·한글 출력 확인, 비압축 메뉴 문자열 깨짐 확인 |

빌드 결과:

```
font cells hijacked : 2350
records rebuilt     : 273 (159 relocated)
messages translated : 3977
bogus records skipped: 11
free space used     : 81636 / 262144 bytes
```

현재 최종 통합 빌드: `build/digicomm_nyo_kr_final.gba`

현재 빌드 SHA-256:
`86EDB44B0DAF3C6BE51811BDB3245390A4D2C1EB5F6295F288E138FCBAFBF48F`

원본 ROM부터 새로 클린 빌드한 `digicomm_nyo_kr_final.gba`의 크기는 16,777,216 bytes다. 직전 버튼 정상본 `digicomm_nyo_kr_final_graybuttons.gba`와 비교하면 2,858 bytes가 달라지지만, 전부 8x8 MFM count `0xDD6810..0xDD6811`과 재배치된 8x8 glyph blob `0xFDC000..0xFDE000` 안에만 있으며 허용 범위 밖 변경은 0 bytes다.

기준 베이스는 최신 메시지 + `cardName.json` 기준 카드 이미지 + 새 종료 버튼 + 최종 미니게임 상태 라벨이 반영된 `build/digicomm_nyo_kr_cards_messages_exit_final.gba`이며, 마지막에 `tools/gba_nameentry_batchim.py`를 적용해 받침 2단계 조합과 포커스 확대 글리프를 추가한다. 이 단계는 `gba_build.py` 이름 입력 파이프라인에도 영구 연결되어 재빌드 시 유지된다.

## 2026-08-31 미니게임 캐릭터명 8x8 받침/누락 글리프 수정

`홋케미린`의 `홋`, `마진갓파`의 `갓`이 미니게임에서 사라지는 원인은 번역 문자열이 아니라 별도 8x8 MFM `0xDD6808`의 글리프 누락이었다. 두 이름은 `messages.json`의 `0xD152CC / 0xD156A0`에 정상 번역되어 있었고 10x10/12x12 폰트에는 해당 코드가 있었지만, 기존 `gba_nameentry_font8.py`는 이름입력 그리드와 고정 raw UI에 필요한 음절만 추가해서 조합형 받침 음절을 놓쳤다.

같은 캐릭터명 구간 17명을 전수 검사해 누락된 `홋(0x99C3) / 갓(0x88AC) / 뭐(0x8F7D) / 밀(0x8FA7)` 4개를 확인했다. `build_invariants.json`의 `raw_ui_text.extra_8x8_glyph_strings`에 캐릭터명 17개를 정책으로 명시하고 `gba_nameentry_font8.py`가 이 문자열의 한글 음절도 자동 수집하도록 수정했다. 8x8 MFM count는 596 -> 600이 되었고 캐릭터명 17개에 사용되는 한글 고유 음절 35개는 최종 ROM에서 누락 0개다.

최종 ROM의 네 새 엔트리는 생성 비트맵과 byte-for-byte 일치한다: `홋=08773e087f083600`, `갓=7213224208142200`, `뭐=7a4a7a027a161200`, `밀=7a4a7a3e0e303e00`. 직전 정상본 대비 변경 2,858 bytes는 전부 `0xDD6810..11` 및 `0xFDC000..0xFDE000` 안이며 허용 범위 밖 변경은 0 bytes다. 원본 ROM부터 클린 전체 빌드 성공. final SHA-256: `86EDB44B0DAF3C6BE51811BDB3245390A4D2C1EB5F6295F288E138FCBAFBF48F`.

## 2026-08-31 명함 보기 하단 버튼 3종 원본 회색 디자인 복원 + `정렬`

사용자 확인 기준을 다시 바로잡았다. 기준은 중앙의 큰 `명함 보기` 핑크 버튼이 아니라 `assets/obj_labels/nameentry_buttons/png/button_F5FB80_32x16.png` 등 **각 일본어 원본 32x16 PNG 자체의 디자인**이다. 이전 빌드가 원본 logical palette를 런타임 bank-9 palette로 저장하면서 원본 폴더 PNG까지 갈색으로 보이게 만들었고, 이후에는 이를 다시 핑크 계열로 바꾸는 잘못된 특례가 들어갔다.

최종 문구:

- `0xF5FB80`: `취소`
- `0xF5FE8C`: `삭제`
- `0xF5FF90`: `정렬`

일본어 원본 PNG의 실제 logical palette는 `0/3/5/12/13/15`이며, 핵심 색은 index 3 = RGB `(213,213,213)` 회색 face, index 12 = `(131,131,131)`, index 13 = `(189,189,189)`, index 15 = 흰색이다. 세 원본 PNG의 픽셀 데이터는 보관 검증본 `build/button_controls_restyle_original`과 3/3 완전 일치하며, 바깥 프레임/베벨은 번역 PNG와 비교해 변경 0픽셀이다.

실제 게임은 같은 OBJ palette bank 9를 B/SELECT/START 아이콘과 공유하므로 bank 전체를 바꾸지 않는다. 아이콘이 사용하지 않는 private index `5/10/12/13`으로 세 텍스트판만 remap한다.

- logical `3` -> runtime `10` = `0x6B5A` = RGB `(213,213,213)`
- logical `5` -> runtime `12` = `0x5F5E` = RGB `(246,213,189)`
- logical `12` -> runtime `5` = `0x4210` = RGB `(131,131,131)`
- logical `13` -> runtime `13` = `0x5EF7` = RGB `(189,189,189)`
- logical `15` -> runtime `15` = 흰색 유지

`tools/gba_patch_rom_buttons.py`는 이제 PNG/preview에는 원본 logical palette를 그대로 쓰고, ROM에 넣을 때만 위 runtime remap을 적용한다. 따라서 `translated_png`의 색과 실제 게임에서 bank 9를 적용한 RGB 결과가 3/3 픽셀 단위로 일치한다.

검증:

- `button_F5FB80_32x16.png` -> `취소`: 원본 바깥 디자인 변경 0픽셀, runtime RGB match PASS
- `button_F5FE8C_32x16.png` -> `삭제`: 원본 바깥 디자인 변경 0픽셀, runtime RGB match PASS
- `button_F5FF90_32x16.png` -> `정렬`: 원본 바깥 디자인 변경 0픽셀, runtime RGB match PASS
- B / SELECT / START raw OBJ: 일본 원본과 byte-for-byte 동일
- 직전 핑크 최종본 대비 차이: 74 bytes, 모두 허용된 viewer 버튼/palette 범위
- 원본 ROM부터 클린 전체 빌드 성공
- final ROM: `build/digicomm_nyo_kr_final.gba`
- SHA-256: `C9BFC2025808BC9426AEC6C9C5E00877FC83EE3FCAA7756C3B81BF0FA1217F01`
- size: `16,777,216 bytes`

## 2026-08-31 명함 보기 하단 버튼 3종 핑크 색상 통일 + `정렬` (폐기 기록)

첨부된 실제 화면과 `state1` OAM/PRAM을 다시 대조했다. 하단 `취소/삭제/정렬` 텍스트판은 OAM #91/#93/#95, **OBJ palette bank 9**를 사용하고 있었고, 중앙의 큰 `명함 보기` 버튼은 OAM #122~#127의 다층 OBJ이며 메인 핑크 레이어는 **palette bank 13**을 사용한다. bank 13의 실제 원본 팔레트는 ROM `0xF8CA94`에 유일하게 존재한다.

사용자가 말한 "원본색"은 하단 일본어판의 갈색을 보존하라는 뜻이 아니라, 화면의 다른 버튼과 같은 색 계열로 맞추라는 의미였으므로 하단 세 버튼을 중앙 버튼과 같은 핑크 계열로 통일했다.

최종 문구:

- `0xF5FB80`: `취소`
- `0xF5FE8C`: `삭제`
- `0xF5FF90`: `정렬`

구현은 B/SELECT/START가 함께 쓰는 bank 9 전체를 바꾸지 않는다. 세 아이콘이 사용하지 않는 index `5/10/12/13`만 중앙 버튼 bank-13의 실제 핑크 색으로 복사하고, 하단 텍스트판의 갈색 face index `3`만 전용 face index `10`으로 remap한다.

- index 5: RGB `(197,74,106)` — 진한 핑크 edge
- index 10: RGB `(255,156,156)` — 메인 face
- index 12: RGB `(255,98,98)` — 선명한 핑크
- index 13: RGB `(255,205,205)` — 밝은 highlight
- 글자: index 15 = 흰색 유지

검증:

- 중앙 `명함 보기` 메인 핑크 donor palette: ROM `0xF8CA94`, state1 bank 13과 일치
- 하단 버튼: state1 OAM #91/#93/#95, bank 9 확인
- B / SELECT / START raw OBJ bytes: byte-for-byte 동일
- B / SELECT / START는 변경 index `5/10/12/13`을 사용하지 않음
- 직전 원본색 빌드 대비 변경 **583 bytes**, 허용된 버튼 3종 + bank-9 네 palette entry 밖 변경 **0 bytes**
- 생성 `translated_png`도 실제 런타임 핑크 palette로 저장
- 원본 ROM부터 클린 전체 빌드 성공
- `build/digicomm_nyo_kr_final_buttoncolor.gba`와 직접 적용 probe가 byte-for-byte 동일
- final ROM: `build/digicomm_nyo_kr_final_buttoncolor.gba`
- SHA-256: `E805439AAF74D776F7DEEC3847358E15AE37F081B820165E855FF293527CA1C2`
- size: `16,777,216 bytes`

## 2026-08-31 명함 보기 하단 조작 버튼 3종 번역 (이전 기록)

최신 `build/digicomm_nyo_kr_final.ss1`의 실제 OAM/VRAM을 기준으로 명함 보기 화면 하단에 남아 있던 일본어 조작 안내를 추적했다. 중앙의 `명함 보기` 큰 버튼은 이미 한글화되어 있었고, 실제 누락은 별도 32x16 raw OBJ 텍스트판 세 개였다.

- `0xF5FB80`: `もどる` → `뒤로`
- `0xF5FE8C`: `回転` → `회전`
- `0xF5FF90`: `近づく` → `확대`
- `0xF5F8F4`: B 아이콘 — 원본 유지
- `0xF5F978`: SELECT 아이콘 — 원본 유지
- `0xF5FA7C`: START 아이콘 — 원본 유지

기존 `tools/gba_patch_rom_buttons.py`의 32x16 raw OBJ 렌더 규칙에 세 텍스트판을 추가했고, `build_invariants.json`의 `nameentry_buttons.expected_translated`도 6 → 9로 올렸다. 따라서 앞으로 클린 빌드에서도 자동 반영된다.

최종 클린 빌드 검증:

- raw button sprites: **9개 자동 패치**
- translated PNG count: **9**
- `0xF5FB80 / 0xF5FE8C / 0xF5FF90`: 최종 ROM 바이트가 생성 indexed PNG와 각각 byte-for-byte 일치
- `0xF5F8F4 / 0xF5F978 / 0xF5FA7C`: 일본 원본과 byte-for-byte 동일
- 친구 대전 런타임 예외 `0xACA74C..0xACB303`: 일본 원본과 byte-for-byte 동일 유지
- final ROM: `build/digicomm_nyo_kr_final.gba`
- SHA-256: `14BE4892928C1C7AA370FEFD792F501F1CB849EA38D6F11BD98F6DD63B74A84B`
- size: `16,777,216 bytes`

주의: mGBA save state는 VRAM도 직렬화하므로 `state1`을 새 ROM에 그대로 로드한 직후에는 저장 당시 일본어 OBJ 타일이 먼저 복원될 수 있다. 이번 state는 누락 주소와 원문 식별 기준으로 사용했고, 최종 검증은 클린 빌드 ROM의 해당 raw OBJ 바이트와 생성 PNG의 직접 대조로 수행했다.

## 2026-08-30 카드효과 2줄 상태 라벨 최종 확정

사용자 실기에서 `가짜 호객`이 1줄 상태(`호객/판매/정돈/회원`)와 달리 뭉개져 보이는 문제를 다시 추적했다. `tools/gba_patch_minigame_photoshopfix.py`의 주석은 area-OR 축소를 제거했다고 되어 있었지만 실제 `render_effect_reference_style()`은 `scale_cropped_mask()`를 계속 호출하고 있었고, 8x8/10x10 골격을 6px 높이로 area-OR 축소하면서 내부 빈 공간과 획이 합쳐지는 것이 직접 원인이었다.

최종 규칙:

- `0x40..0x43` / `0x54..0x57`: `가짜 호객`
- `0x44..0x47` / `0x58..0x5B`: `가짜 회원`
- `0x48..0x4B` / `0x5C..0x5F`: `가짜 판매`
- `0x4C..0x4F`: `가드`
- `0x50..0x53`: reserved blank 유지
- 2줄 `가짜 *`는 generic scaling/area-OR를 **절대 사용하지 않는다**. 실제 1줄 상태용 native 8x8 한글 strike에서 글자별로 명시한 5개 scanline만 선택해 compact glyph를 만든다.
- 윗줄 outline bbox는 `y=0..6`, 아랫줄 outline bbox는 `y=8..14`; `y=7`은 완전 공백이어야 하며 빌드 시 하드 검증한다.
- palette bank13의 `5=남색 외곽선`, `F/D/9=밝은 면 수직 그라데이션`을 사용해 1줄 상태와 같은 픽셀 스타일을 유지한다.
- primary/duplicate 그룹은 4셀 pixel payload가 byte-for-byte 동일해야 하며 다르면 빌드 실패한다.
- `가드`는 native 10x10 `가/드` strike의 빈 여백만 crop하고 scaling은 하지 않는다.

`state1`을 실제 mGBA에 로드하고 입력을 전혀 보내지 않은 채 약 5초 진행했을 때 카드효과 `가짜 호객`이 32x16, palette bank13 OBJ로 활성화되는 것을 확인했다. 수정 후 OAM/VRAM의 해당 OBJ가 새 0x40 그룹 MFM 픽셀과 완전 일치했고, 사용자가 실기 화면에서 `가짜 호객`이 정상 적용된 것을 최종 확인했다. 나머지 `가짜 회원/가짜 판매`는 같은 렌더 함수와 동일 하드 invariant를 사용하며 중복 세트도 byte-identical로 생성된다.

최종 패처 기본 입출력도 최신 통합본 기준으로 변경했다.

- tool: `tools/gba_patch_minigame_photoshopfix.py`
- default base: `build/digicomm_nyo_kr_cards_messages_exit.gba`
- final ROM: `build/digicomm_nyo_kr_cards_messages_exit_final.gba`
- report: `build/digicomm_nyo_kr_cards_messages_exit_final.json`
- SHA-256: `1BEFC4B0544D0C4A3FDF589FE04CD3BA28B253EF402BC8C59A7ABA9E18C2BF04`
- 최신 통합 베이스 대비 변경: **1,013 bytes**
- whitelist 밖 변경: **0 bytes**
- `가짜 호객/회원/판매`: `separator_row_7_blank=true`, duplicate groups identical
- `0x50..0x53`: blank PASS


## 2026-08-30 미니게임 HUD/목표 문구 후속 수정

- `0xB36C60`은 사용자 `0b36c60_240x16022355.png`를 원본 **167타일 + Huffman→RLE + 원본 Huffman tree/섹션 경계**로 넣은 exact-tree 판을 유지한다.
- 미니게임 전용 `0xD7CBC4` 글리프 시트는 원본 Huffman4 tree/4섹션을 유지하면서 `あと→앞으로`, `円→엔`, `人→명`, `日→일`, `万→만`을 적용한다. `15=万`, `16=億`이며 이전의 `15=차,16=만` 해석은 오류였다.
- 상단 날짜/머리 위 업무 상태의 구형 일본어 코드 경로를 대비해 **10×10 미니게임 MFM(`0xD83730`)만** `日目→일차`, `よび→호객`, `整と→정돈`, `販売→판매`, `会員→회원`으로 글리프 오버라이드한다. 일반 12×12 대화 폰트는 건드리지 않는다.
- `0xD00000~0xD80000` 미니게임 스크립트 안의 `숫자+万円`, `숫자+人`을 바이트 길이 동일한 `숫자+만엔`, `숫자+명`으로 전수 치환한다. 현재 94곳.
- 후처리 도구: `tools/gba_patch_minigame_textfix_final.py`
- 현재 테스트 ROM: `build/digicomm_nyo_kr_minigame_textfix_final.gba`
- SHA-256: `46472439ED7300A2F75EEC79A4993E38830C02DC7E8BC8CA4D6AFB0BEA4E1D78`
- `0xB36C60` exact-tree 영역과 `0xD7CBC4` 전용 글리프 영역은 직전 정상판과 바이트 단위 동일. 600프레임 저장상태 회귀 실행에서 중단/invalid-address 없음.
- **주의:** mGBA의 이전 ROM에서 만든 세이브스테이트는 VRAM/텍스트 객체를 같이 복원하므로 `日目/販売/会員` 같은 이미 렌더된 문구가 일본어로 계속 보일 수 있다. 표시 검증은 배터리 세이브/일반 세이브를 사용해 새 ROM에서 미니게임에 새로 진입해야 한다.

## 메인 메뉴 이동·선택 정지 수정

2026-08-27 정적 비교에서 메인 메뉴 레코드 `0xD19E74`가 번역 후
1,736바이트에서 1,560바이트로 줄어든 것을 확인했다. 메시지 뒤 메뉴 이동·선택
제어 데이터가 176바이트 앞당겨지는 것이 정지 원인이었다. 같은 메시지 헤더·슬롯
배열을 가진 구조적 쌍은 `0xD19E74`, `0xD1A2AC` 두 개뿐이었다.

두 레코드를 빌더의 고정 메시지 슬롯 대상에 추가하고 넘치던 6문장을 축약했다.
새 빌드에서 두 레코드 모두 1,736바이트, 41개 메시지의 `(header, length)` 배열은
원본과 동일하며 메시지 밖 변경은 0바이트다. 기존 첫 만남 선택지 `0xD303FC`도
같은 검사로 1,736바이트, 25개 슬롯 동일, 메시지 밖 변경 0바이트를 재확인했다.
정적 검사에서 원인이 확정되어 emucap 동적 추적은 수행하지 않았다.

## 2026-08-30 state1 미니게임 잔존 일본어 수정

사용자 mGBA state1 `build/digicomm_nyo_kr_minigame_textfix_final.ss1`의 PNG `gbAs` 청크를 직접 해제해 PPU/VRAM을 확인했다. state1은 최종 ROM 생성 후 저장된 최신 상태이며, 상단 날짜와 캐릭터 머리 위 상태는 OBJ나 정적 MBM/MCM 이미지가 아니라 BG0 8bpp 런타임 텍스트 타일이었다.

확정 원인과 수정은 다음과 같다.

- 캐릭터 상태 `よび / 整と / 販売 / 会員`: `MFM 0xDD6808`의 8x8 미니게임 전용 폰트가 원 일본어 코드를 직접 사용한다. 확장 한글 코드만 추가해서는 바뀌지 않으므로 원 슬롯을 `호객 / 정돈 / 판매 / 회원` 비트맵으로 직접 교체한다.
- 상단 `日目`: 10x10 원 슬롯은 이미 바뀌어 있었지만 실제 표시용 12x12 `MFM 0xDA8DD4`의 `日/目` 원 슬롯이 일본어로 남아 있었다. 이를 `일/차`로 교체한다.
- 상대 가게명 `ブラックゲーマーズ`: 일반 상점명 `0xAB1514`가 아니라 미니게임 스테이지 정의에 43개 고정 문자열이 별도로 남아 있었다. 18바이트 슬롯을 유지한 채 `블랙 게이머즈` + 공백 패딩으로 43곳 모두 교체한다.

재현 패처: `tools/gba_patch_minigame_state1_remaining.py`
현재 테스트 ROM: `build/digicomm_nyo_kr_minigame_state1fix.gba`
SHA-256: `6650F35514EF346FA742B31E368E827190A991092944748884B02C4B7BA2724A`

정상 동작이 확정된 `0xB36C60` exact-tree 영역과 `0xD7CBC4` 미니게임 HUD atlas는 직전 정상 ROM과 바이트 단위 동일하다. Mesen 저장 상태 기준 600프레임 회귀 테스트도 invalid-address 없이 통과했다.

## 2026-08-30 미니게임 `일차`/상태 라벨 원본 스타일 재작업

사용자 피드백에 따라 이미 한글화된 `일차`, `호객`, `판매`, `정돈`, `회원`의 글자 디자인을 다시 손봤다. 기존 `tools/gba_patch_minigame_state1_remaining.py`는 상태 라벨을 실질적으로 `5=남색 외곽선 / D=연한 파랑 / F=흰색` 3색으로만 평면 렌더링해 원본 일본어보다 계조가 적었고, 특히 `판매`의 조밀한 획이 뭉개져 보일 수 있었다. 날짜 `일/차`도 원본 `日/目`의 회색 베벨을 쓰지 않았다.

원본 state1 팔레트를 다시 읽어 실제 색 역할을 그대로 사용하도록 렌더러를 수정했다.

- 상태 라벨 palette bank 13: `5 / 1 / 2` = 남색~라벤더 외곽/그림자, `9 / D / F` = 회청색~흰색 면
- 날짜 palette bank 7: `3` = 검정 외곽, `B` = 회색 하단/우측 베벨, `2` = 연한 파랑 면 그림자, `C` = 흰색 면
- 상태 글자는 원본 밀도에 가까운 `Galmuri9Bitmap` 단일 strike를 사용하고 24x16 안에서 하드 픽셀 마스크로 렌더한다.
- FreeType 회색 프린지는 128 threshold로 제거해 4bpp에서 잡색/부풀림이 생기지 않게 했다.
- `판매`는 3개 8x16 분할 타일 모두 잉크가 존재함을 확인했다: `[69, 79, 65]` pixels.
- `호객/정돈/회원`도 각 3타일 모두 비어 있지 않다.
- 날짜 `일/차` 타일 잉크는 각각 `56 / 58` pixels.

새 테스트 ROM:
`build/digicomm_nyo_kr_minigame_stylefix.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `2ECAA85C972DB12B9407A1DC392488F8D3FE6B87EA1DB76124CEDF9CDB542815`

### 2026-08-30 stylefix3 고대비 재수정

`stylefix2`는 한글 비트맵 자체는 들어가 있었지만 상태/날짜 글리프를 지나치게 단순화해 실기에서 희미하게 보일 수 있었다. `tools/gba_patch_minigame_stylefix3.py`를 추가해 최신 `messages.json`이 반영된 `digicomm_nyo_kr_message_minigame_stage.gba`를 베이스로 다음을 다시 고정한다.

- 캐릭터 머리 위 `호객 / 판매 / 정돈 / 회원`: 24x16 OBJ를 흰 면(F) + 진한 외곽(5) 고대비로 재생성
- 상단 `일 / 차`: 8x8 OBJ를 흰 면(C) + 검정 외곽(3)으로 재생성
- 실제 런타임 12x12 `일/차` MFM 글리프도 획을 1px 보강
- 8x8 상태 MFM 글리프와 블랙 게이머즈 43개 고정 라벨은 직전 정상 번역을 유지
- `0xB36C60` 사용자 이미지 5,932바이트 영역 및 `0xD7CBC4~0xD81758` HUD 영역은 베이스와 byte-for-byte 동일

새 ROM: `build/digicomm_nyo_kr_minigame_stylefix3.gba`
SHA-256: `10E5F309B478FC86F62EB001169C5F6C43446C1407A2ECEE98090CB60773DF38`
Mesen 600프레임 회귀: PASS, invalid-address 없음.
- 자체 검증: status glyph 8 / date glyph 2 / status OBJ entry 12 / date OBJ tile 2 / 블랙 게이머즈 43 labels 모두 PASS
- 직전 `digicomm_nyo_kr_minigame_state1_objfix.gba` 대비 차이: **458 bytes**
- 차이 범위는 `0xD81799~0xD81B5C` 상태 라벨 OBJ와 `0xE3D228~0xE3D267` 날짜 OBJ에만 존재
- `0xB36C60` exact-tree 이미지 영역: 직전판과 byte-for-byte 동일
- `0xD7CBC4` HUD atlas 영역: 직전판과 byte-for-byte 동일
- Mesen 저장상태 600프레임 회귀: PASS, invalid-address 없음

### 2026-08-30 메인 이미지 재회귀 원인 확정 + `일차/상태명` 최종 복구

`stylefix3` 이후 사용자 실기 피드백을 다시 추적한 결과, 두 문제가 모두 후속 빌드의 **베이스 ROM 회귀**에서 발생했음을 바이너리 비교로 확정했다.

- 실기 정상 확인된 `0xB36C60`은 **167타일**, 섹션 `0xB36C98 / 0xB374E4 / 0xB37F14`, 내부 `RLE(0x30) -> Huffman8(0x28)` exact-tree 계약이다.
- 그런데 `digicomm_nyo_kr_message_minigame_stage.gba -> stylefix2 -> stylefix3` 체인이 다시 구형 **165타일**, `0xB36C98 / 0xB374D0 / 0xB37EDC` 판을 베이스로 사용했다. 즉 정상 이미지를 해결하지 못한 것이 아니라, 후속 빌드가 정상판을 다시 덮어쓴 것이 직접 원인이었다.
- 같은 베이스 회귀 때문에 `0xD7CBC4~0xD81758` 미니게임 HUD atlas도 직전 정상판과 달라져 있었다.
- `stylefix3`의 `일차/호객/판매/정돈/회원`은 원본 일본어 스타일을 재도트한 것이 아니라 기존 한글 마스크를 1px 굵게 만든 뒤 `5/F` 두 색으로 다시 칠한 것이어서 획이 뭉치고 원본 6단계 베벨을 잃었다.

이를 대체하는 `tools/gba_patch_minigame_recovery_final.py`를 추가했다. 최신 `messages.json` 반영 베이스를 그대로 두고 아래 소유 범위만 교체하며, 그 밖의 바이트가 하나라도 바뀌면 실패한다.

- `0xB36C60`: 런타임 정상 확인 167타일 exact-tree 판으로 강제 복구
- `0xD7CBC4~0xD81758`: 런타임 정상 확인 HUD atlas로 강제 복구
- 실제 상태 8x8 MFM: `호/객/정/돈/판/매/회/원` 얇은 한글 글리프 유지
- 실제 날짜 12x12 MFM: `일/차`를 원본 `日/目`과 동일한 bbox **`x=3..10, y=0..10`** 으로 맞춤; stylefix3의 굵게 처리 제거
- 24x16 상태 프리렌더 경로: 원본 상태별 여백/bbox를 유지하고 팔레트 **`1/2/5/9/D/F`** 6단계로 재도트
  - `호객`: `(0,3)~(23,12)`
  - `판매`: 원본처럼 왼쪽 여백을 유지해 실제 한글 본체가 오른쪽에 위치
  - `정돈`: `(0,4)~(16,12)`
  - `회원`: `(0,3)~(22,12)`
- 8x8 날짜 fallback: 원본 날짜 팔레트 `2/3/B/C` 유지
- `블랙 게이머즈` 43개 고정 슬롯도 다시 검증

최종 복구 ROM:
`build/digicomm_nyo_kr_minigame_recovery_final.gba`

- SHA-256: `09007D8B04AEA07422E501A82B20232A513DA8A625407FA220B01DD959E4D412`
- `0xB36C60`: **167 tiles**, exact section pointers PASS, nested RLE 3/3 PASS
- `0xB36C60` 정상 범위 SHA-256: `83CDB1586A11FC85D8A965D352C3D6C21D50D81DDD5A68A152388322EF16D1CF`
- HUD 정상 범위 SHA-256: `CEE6EED552F9FB2C7206D86807FBD48CA8AA29E63D3FB33235054A9CE76B3F7A`
- 최신 메시지 베이스 대비 변경: 11,242 bytes, **화이트리스트 밖 변경 0 bytes**
- 저장상태 회귀: **600프레임 PASS**

`assets/build_invariants.json`에도 167타일 exact-tree 계약과 상태/날짜 팔레트·bbox를 하드 불변조건으로 등록했다. 기존 165타일 설명은 폐기한다.

### 2026-08-30 Photoshop 참조 스타일 + 메인화면 물리 경계 수정

사용자 실기 스크린샷으로 `readabilityfix`를 다시 검증한 결과, 두 가지 판단을 정정했다.

- 메인화면 `0xB36C60`은 167타일/섹션 포인터만 맞으면 끝나는 것이 아니었다. 이전 패치는 `0xB38384`까지만 정상본을 복사했지만 실제 64색 palette는 `0xB38874~0xB388F3`에 있다. 따라서 `state1_objfix`에서 다음 MBM 시작점 `0xB388F4` 직전까지 **`0xB36C60~0xB388F3` 7,316바이트 전체 물리 영역**을 복구해야 한다.
- `+`는 상태 폭 제어 바이트가 아니라 원문 `よび+%d` 등의 literal이다. 이전 `readabilityfix`에서 이를 되살린 것은 잘못된 가설이었다. 새 패치는 `state1_objfix`의 한글 상태 문자열 바이트를 그대로 유지한다.
- 캐릭터 위 `호객/판매/정돈/회원`은 사용자 Photoshop/참조 화면처럼 얇은 8x8 한글 core를 서로 떨어뜨려 배치하고, palette bank 13의 `5/1/2/9/D/F`를 사용해 **진한 남색 외곽선 + 흰색 중심 + 아래쪽 연청/라벤더 그라데이션**으로 다시 만든다. core 자체를 dilation하지 않는다.
- `~일차`는 `state1_objfix` geometry를 유지하고 palette bank 7의 `C(흰색) -> 2(연청) -> B(회색)` face bevel + `3(검정)` outline으로 바꿔, 이전의 C→B 단색 어둡게 처리와 반대로 원본처럼 밝은 흰색 중심을 복원한다.
- 최신 `messages.json`은 `digicomm_nyo_kr_message_minigame_stage.gba`를 베이스로 유지하고, 허용된 graphics/font/fixed-label 범위 밖 바이트는 변경하지 않는다.

새 ROM: `build/digicomm_nyo_kr_minigame_photoshopfix.gba`
SHA-256: `57F0D745D6C19313717B7E6F6BE59E732E19DDBDF4A88EE4B207A7AF6FE9097C`

검증:
- B36: 167 tiles, section pointers `0xB36C98/0xB374E4/0xB37F14`, palette `0xB38874~0xB388F3`, 전체 physical span이 `state1_objfix`와 byte-for-byte 동일
- Mesen 동일 입력 시퀀스로 `photoshopfix`와 `state1_objfix`의 타이틀/메인화면 캡처 5장 모두 **pixel-for-pixel 동일**
- 상태 4종은 3개 타일 모두 non-empty, palette `1/2/5/9/D/F` 유지
- 날짜 2종은 palette `2/3/B/C` 유지
- 블랙 게이머즈 43개 PASS, 최신 메시지 베이스 대비 whitelist 밖 변경 0바이트
- 미니게임 저장상태 600프레임 회귀 PASS

### 2026-08-30 상태 글자 좌우반전/가짜 추가문자 수정

사용자 실기에서 `photoshopfix`의 상태 한글이 좌우반전되고 오른쪽 자모가 별도 한자/문자처럼 보이는 문제가 확인됐다. 원인은 8x8 1bpp 한글 strike를 24x16 4bpp 상태 OBJ로 옮길 때 bit 0을 x=0으로 읽은 것이었다. 런타임 폰트 자체는 그 순서로 보존해야 하지만, 4bpp 프리렌더 변환에서는 **MSB-left**로 해석해야 정상 방향이 된다.

- 실제 `STATUS_FONT` ROM 바이트는 `state1_objfix` 그대로 유지한다.
- `호객/판매/정돈/회원` 24x16 프리렌더를 만들 때만 1bpp 각 행을 MSB→LSB 순서로 변환한다.
- 두 번째 글자를 1px 왼쪽으로 옮겨 두 글자 사이 여백은 유지하면서 24px 라벨의 `x=22..23`을 **항상 완전 공백**으로 만든다. 숫자 타일 앞에 자모/그림자가 남아 별도 문자처럼 보이는 것을 금지한다.
- `~일차`, 메인화면 전체 `0xB36C60~0xB388F3`, 최신 메시지, 블랙 게이머즈 43개는 photoshopfix와 동일하게 유지한다.

새 ROM: `build/digicomm_nyo_kr_minigame_photoshopfix2.gba`
SHA-256: `3BC39FAE83C11C151A451B50DB1F856AB32D9C14DD4C2742052B53FDCB6D8611`

검증:
- 상태 4종 모두 right margin `x=22..23` nonzero = **0**
- 상태 4종 모두 3개 프리렌더 타일 non-empty, palette `1/2/5/9/D/F` 유지
- B36 전체 physical span/64색 palette는 `state1_objfix`와 동일
- 최신 메시지 베이스 대비 whitelist 밖 변경 **0바이트**
- 저장상태 600프레임 회귀 **PASS**
- `savestate_shop_gameplay.mss` 로드 후 30프레임 실제 게임 진행에서도 이벤트/크래시 없음

### 2026-08-30 상태 라벨 원본 일본어 geometry 고정

추가 실기 피드백에서 `호객` 숫자 아래 잔여 문자, `정돈` 주변의 다른 글자처럼 보이는 조각, `판매` 잘림이 계속 확인됐다. 공통 22px 한글 박스를 강제하는 접근을 폐기하고 일본어 원본 ROM의 MFM 3조각을 직접 측정했다.

원본 실제 bbox:
- `よび` → `(0,3)~(23,12)`
- `販売` → `(0,3)~(23,12)`
- `整と` → `(0,4)~(16,12)`
- `会員` → `(0,3)~(22,12)`

새 패치는 `호객/판매/정돈/회원`을 각각 위 bbox와 **완전히 같은 크기·위치** 안에서만 렌더링한다. bbox 밖 nonzero pixel은 빌드 실패로 처리한다. 특히 `정돈`은 원본처럼 17x9 영역만 사용한다.

또 원본 상태 포맷 `よび+%d` 등은 `+` 자체는 literal이지만, 숫자 앞 ASCII 셀까지 포함해 7바이트 표시 폭을 가진다. 한글 `호객%d`는 6바이트라 숫자/이전 셀이 한 칸 당겨질 수 있으므로 `+` 대신 **ASCII space**를 넣어 `호객 %d / 판매 %d / 정돈 %d / 회원 %d`로 원본 폭을 유지한다.

새 ROM: `build/digicomm_nyo_kr_minigame_photoshopfix4.gba`
SHA-256: `75688913DB28B28B88D2BE80B5174D3804C506469FA8F6C4CBBD7ABDE3B891C5`

검증:
- 상태 4종 bbox가 일본어 원본 bbox와 exact match
- bbox 밖 nonzero pixel 4종 모두 **0**
- raw status slot 4종 모두 7 visible/control bytes + NUL, ASCII space 포함
- B36 전체 physical span/64색 palette 유지
- 최신 메시지 베이스 대비 whitelist 밖 변경 **0바이트**
- 미니게임 저장상태 600프레임 회귀 **PASS**

### 2026-08-30 State 3 기준 상태 MFM 4-cell 매핑 수정

사용자가 `photoshopfix5.ss3`에 문제 장면을 저장해 준 뒤 실제 OAM/VRAM을 역추적해, 이전 패치의 근본 매핑 오류를 확정했다. 상태명은 24x16 세 칸만 독립적으로 쓰는 것이 아니라 **32x16 / 4-cell 단위**이며 네 번째 칸 위에 숫자가 그려진다.

실제 런타임 그룹:
- `호객`: `0x30,0x31,0x32` + numeric `0x33`
- `판매`: `0x34,0x35,0x36` + numeric `0x37`
- `정돈`: `0x38,0x39,0x3A` + numeric `0x3B`
- `회원`: `0x3C,0x3D,0x3E` + numeric `0x3F`

이전 매핑은 판매를 `0x33~0x35`, 정돈을 `0x39~0x3B`로 한 칸씩 잘못 패치했다. 그 결과 판매가 잘리고, 정돈 첫 칸 `0x38`의 일본어가 남았으며, 잘못 패치된 `0x33/0x3B` 상태 픽셀이 숫자 밑에 그대로 남았다.

`photoshopfix6`에서는 사용자가 기준으로 지정한 `photoshopfix3`의 `호객/판매/정돈/회원` 24x16 픽셀 모양을 **byte-for-byte 그대로** 유지해 실제 첫 3칸으로 재배치하고, 네 번째 numeric cell은 완전 공백으로 초기화한다. 또한 원본 포맷 `よび+%d` 등의 `+`(0x2B)는 삭제/공백 치환하지 않고 `호객+%d / 판매+%d / 정돈+%d / 회원+%d` 구조로 그대로 보존한다.

새 ROM: `build/digicomm_nyo_kr_minigame_photoshopfix6.gba`
SHA-256: `DD112AE0B106276C945B48259D6BFF2E25A18D364A960E227982645132BAAE6E`

State 3 교정본: `build/digicomm_nyo_kr_minigame_photoshopfix6.ss3`
SHA-256: `720C3E68CF3484782B7BA1305B678C2B77B5867B58BA366FB8926CAA7F6A7424`

검증:
- 네 상태 모두 photoshopfix3 24x16 shape exact match
- 판매 runtime code `0x34~0x36`, 정돈 runtime code `0x38~0x3A`로 수정
- numeric fourth cell 4종 ROM 초기값 nonzero = **0**
- `+` 4개 raw slot 모두 `0x2B` 보존
- State 3 변환에서 정돈 numeric 잔여 44px, 호객 numeric 잔여 66px 제거
- mGBA에서 fix6.ss3 로드 후 실제 런타임 재생성 검증: 정돈=`38/39/3A`, 회원=`3C/3D/3E`, 호객=`30/31/32`, 세 numeric tile rows 10~15 잔여 **0**
- 최신 메시지 베이스 대비 whitelist 밖 변경 **0바이트**

### 2026-08-30 카드효과 상태 라벨 0x40~0x5F 전수 한글화

State 2 실기 저장상태에서 기본 업무 상태와 별개인 32x16 OBJ가 MFM `0x40~0x43`을 사용하며, 카드 `にせよびこみ`(가짜 호객) 표시임을 확인했다. 전체 MFM을 감사한 결과 카드효과 라벨 범위는 `0x40~0x5F`이고 중복 리소스까지 포함한다.

- `0x40~0x43`, `0x54~0x57` → `가짜 호객`
- `0x44~0x47`, `0x58~0x5B` → `가짜 회원`
- `0x48~0x4B`, `0x5C~0x5F` → `가짜 판매`
- `0x4C~0x4F` → `가드`
- `0x50~0x53` → 원본과 동일하게 blank 유지

가짜 3종은 32x16에서 윗줄 `가짜`, 아랫줄 `호객/회원/판매`의 2줄 구조로 만들고, `photoshopfix3`와 동일한 palette bank13 `5/9/D/F`(남색 외곽선 + 흰색→연청/라벤더 face bevel)를 사용한다. 기본 `호객/판매/정돈/회원`은 `photoshopfix3` 24x16 모양 byte-for-byte 계약과 올바른 4-cell 매핑, 숫자칸 blank, raw `+`(0x2B) 보존 정책을 그대로 유지한다.

새 ROM: `build/digicomm_nyo_kr_minigame_photoshopfix7.gba`
SHA-256: `A7954C4EB943D5A1C503509CB7A178E51CDC28970B6C5C73F6897FE61280CFB5`

사용자가 저장한 State 2도 `build/digicomm_nyo_kr_minigame_photoshopfix7.ss2`로 변환했다. mGBA에서 0.1초 실제 실행 후 OAM/VRAM을 재검증해 `판매 / 회원 / 가짜 호객`이 모두 fix7 MFM과 exact match하는 것을 확인했다. 일반 미니게임 회귀는 600 frames PASS.

## 빌드

```bash
python tools/gba_build.py \
  --rom  "D:/game/gba/DigiCommunication Nyo - Datou! Black Gemagema Dan (Japan)/DigiCommunication Nyo - Datou! Black Gemagema Dan (Japan).gba" \
  --messages work/digicomm_nyo/assets/translation/messages.json \
  --used     work/digicomm_nyo/analysis/used_sjis.json \
  --out      work/digicomm_nyo/build/digicomm_nyo_kr.gba \
  --out-map  work/digicomm_nyo/build/hangul_map_built.json
```

`gba_build.py` 하나가 폰트 패치 → 레코드 해제 → 메시지 재작성 → **원본과 같은
형식·같은 중첩으로** 재압축 → 자리에 맞으면 제자리, 넘치면 `0xFC0000~0x1000000`
으로 이전 + 셀프포인터 갱신까지 전부 한다. 단계를 밖에 두면 깨끗한 빌드에서
조용히 빠진다.

빌드 불변조건은 `assets/build_invariants.json`이 소유한다. 이제 일부 예외 목록이
아니라 **모든 재삽입 가능 메시지 레코드**의 원본 슬롯 위치·길이를 매 빌드에
강제한다. 번역 payload 밖의 바이트가 하나라도 바뀌어도 빌드를 실패시킨다.
`japanese_images/translated_png`의 이미지 재압축·재삽입·역추출 픽셀 검증도
단일 빌드 진입점에 통합했다. 수동 덮어쓰기가 아니라 원래 MCM 코덱과 섹션 수를
유지하며, 번역 PNG와 ROM에서 다시 그린 결과가 다르면 빌드를 실패시킨다.

### 2026-08-27 전체 전환 멈춤 근본 수정

메시지 길이를 가변 재작성하면 같은 해제 레코드 뒤쪽의 분기·이동 데이터 위치가
밀리는 것이 공통 원인이었다. 특정 메뉴나 미니게임만의 문제가 아니므로 기존 번역
중 원본 슬롯을 넘던 737개를 `gemma-4-26b-a4b-it-qat`로 간결화하고, 빌드 정책을
`fixed_message_slot_mode: all_reinsertable`로 전환했다. 말버릇은 앞말에 붙이고,
제어 토큰과 숫자를 보존한 뒤 실제 인코딩 바이트로 다시 검사한다.

`cache/audit_fixed_slots.py`로 최종 ROM의 273개 레코드/3,977개 번역 메시지를 원본과
대조한 결과, 슬롯 초과 0개, 메시지 밖 변경 0바이트였다. 300인 선착순 미니게임이
있는 `0xD7298C`도 통과했다. 정적으로 원인이 확정되고 전체 구조 불변식까지 검증되어
emucap 동적 추적은 수행하지 않았다.

현재 검증 빌드 SHA-256:
`E645B4B37FEA5079FBABEB97A36AD5B84C22BAF60CD9E22A1A3D3220652D598C`

## 번역

로컬 LM Studio (`http://127.0.0.1:1234`, `gemma-4-26b-a4b-it-qat`).
캐시(`cache/translation_cache.json`)가 진실의 원천이며 **양방향으로 권위가 있다** —
캐시에 없는 번역은 `messages.json`에서 지운다. 한쪽만 반영하면 폐기한 번역이
파일에 남는다(실제로 216건을 폐기했는데 130건이 남아 있었다).

검증을 통과한 것만 채택하므로 재실행은 후퇴시키지 않는다. 검사 항목:

- 폰트에 없는 한글 음절 (KS X 1001 2,350자 밖)
- 반각 영숫자 (글리프 없음 — 전각으로 자동 교정)
- 치환 기호 `%s` `%d` `$1` 보존
- 원문의 비일본어 문자 `～ ♪ ！ ？ 「」 『』 【】 ・ … （）` 보존
- **화자 말끝 어미** (아래)
- 가나 잔존

### 화자 말끝 어미

이 게임은 캐릭터를 말끝으로 구분한다. 어미가 바뀌면 **말풍선에 다른 인물
목소리가 들어간다.** `ゲマよ！`를 데지코 말투 `~다뇨！`로 옮긴 곳이 있었다.

| 원문 | 한국어 | 인물 |
|---|---|---|
| `ゲマ` | 게마 | 게마 |
| `ゲバ` | 게바 | 게바게바 |
| `にょ` | 뇨 | 데지코 |
| `にゅ` | 뉴 | 푸치코 |
| `ぴょ` | 뾰 | 피요코 |

한국어 말버릇은 앞말과 붙여 쓴다. 예: `한다뇨`, `한다뉴`, `한다뾰`,
`한다게마`, `한다게바`. `gba_translate.py`가 문장 끝 말버릇 앞 공백을 자동으로
제거하고, 검증기 역시 띄어 쓴 형태를 거부한다.

검사 도입 전 어미가 있는 원문 1,482개 중 **27개(1.8%)가 불일치**했다
(`ゲマ`→소실 13, `にゅ`→`뇨` 5, `ゲマ`→`뇨` 4, `にょ`→소실 4, `ぴょ`→소실 1).
프롬프트에 줄마다 요구 어미를 명시하고 검증기에 추가해 해결했다. 용어집에
적어 두는 것만으로는 부족하다.

### 줄바꿈은 원문을 따른다 (중요)

**한 줄에 8자로 고정하고 접던 것은 틀렸다.** 두 가지 이유로:

- 원문 4,032줄 중 **2,094줄(52%)에 개행이 아예 없다.** 게임이 알아서 접는
  줄인데 거기에 `\n`을 박으면 게임의 줄바꿈을 덮어쓴다.
- 개행이 있는 원문의 최장 줄 폭은 **8자 564건, 12자 373건** 으로 갈린다.
  말풍선이 최소 두 종류다. 실측한 8자짜리를 전체에 적용하면 12자 대사는
  상자를 3분의 1 버린다.

현재 규칙 — 원문에 개행이 없으면 번역도 없고, 있으면 **원문 자신의 최장 줄
폭**으로 접는다. 결과: 78%가 원문과 줄 수까지 일치, +1줄 648건, +2줄 79건.

### 원문의 개행은 단어 중간을 자른다

저자가 손으로 상자에 맞춰 접었기 때문에 `相手の店` / `員をみんな` 처럼 `店員`이
반으로 갈린다. **그 조각을 그대로 모델에 넘기면 조각으로 번역된다** — `점원을`이
아니라 `점 원을`. 기존 8자 줄바꿈이 우연히 같은 자리를 잘라서 오래 가려져 있었다.

`unwrap_source()`가 원문의 개행을 두 종류로 가른다:

```python
if len(out[-1]) >= width - 1:
    out[-1] += line      # 폭 끝까지 갔다 → 접힌 것, 이어붙인다
else:
    out.append(line)     # 짧게 끝났다 → 저자가 의도한 개행, 남긴다
```

개행을 전부 떼면 `【成功】` 같은 머리줄까지 문장에 합쳐지므로 이 구분이 필요하다.

### 이미 접힌 캐시는 재번역 없이 되돌렸다

`tools/gba_rewrap_cache.py`. 옛 `wrap()`이 결정적이므로 줄바꿈 지점을 길이로
역판별한다:

| 앞줄 | 판정 |
|---|---|
| 폭보다 짧고 다음 단어가 **들어갔을** 자리 | 모델이 쓴 문단 개행 |
| 폭보다 짧고 다음 단어가 **안 들어감** | 단어 경계에서 접힘 → 공백을 먹었다 |
| 폭에 정확히 참 | 판별 불가 → 3후보를 옛 `wrap()`으로 재생해 대조 |

복원한 텍스트를 옛 알고리즘에 다시 넣어 캐시와 바이트 단위 일치를 전수 검사했다.
**3,537개 전부 일치, 실패 0.** 재번역이 필요했던 것은 조각 번역이 섞인
"원문에 개행이 있는" 1,754개뿐이었다.

## 폰트

`0x00DA8DE8`–`0x00DD514A`, 6,965엔트리 × 26바이트 = SJIS 코드 u16(빅엔디언) +
12행 × u16 1bpp 글리프. 잉크 박스 y=0..10, x=1..11, 획 1px. **2바이트 SJIS만
있고 반각 글리프는 없다.**

게임이 안 쓰는 한자 셀 5,429개 중 2,350개를 징발해 KS X 1001 완성형 전체를
**미리 배정**한다. 필요할 때 배정하면 번역을 고칠 때마다 매핑이 밀린다.

기본값: **얇은물마루 v1.1** (`assets/fonts/thinmulmaru/font/ThinMulmaru.ttf`),
인덱스 0, 12px, 임계 110, **dy=0**. 12px의 실측 잉크 x=0..10,
y=0..10이 게임의 11x12 셀과 원본 폰트 높이에 그대로 맞는다. 고정폭판은 일부
글자가 오른쪽 경계를 넘으므로, 빌더가 고정 셀로 만드는 가변폭판을 사용한다.
KS X 1001 2,350자를 전수 검사해 셀 밖 글리프 0개, 빈 글리프 0개를 확인했다.
출처·OFL-1.1 라이선스·파일 해시는 `assets/fonts/thinmulmaru/README.md`에
기록했다. emucap 화면 검증은 사용자 요청으로 생략했다.

## 선택지 정지 수정

첫 만남 장면의 `혹시, 방금 데지코의 혼잣말을 들은 거냐뇨？` 뒤에서 `예`와
`아니요` 어느 쪽을 골라도 정지하는 문제를 수정했다. 압축 레코드 `0xD303FC`는
텍스트 뒤의 선택지 제어 데이터가 원래 해제 데이터 내 위치에 의존한다. 기존
가변 길이 재삽입은 텍스트 길이 변화만큼 이 제어 데이터를 이동시켜 분기 대상을
깨뜨렸다.

2026-08-27 재확인 결과, 이 설명과 달리 실제 단일 빌드 진입점 `gba_build.py`에는
고정 슬롯 처리가 빠져 있어 문제가 계속 재현될 수 있는 상태였다. 빌더에
`0xD303FC` 고정 슬롯 처리를 직접 넣고, 슬롯을 넘던 번역 2개를 의미를 유지하는
범위에서 줄였다. 빌드 시 슬롯을 넘으면 즉시 실패하며, 레코드 전체 길이가
바뀌어도 실패한다. 새 빌드에서 원본과 재구축 데이터는 모두 1,736바이트,
25개 메시지의 `(header, length)` 배열은 완전히 동일하고 메시지 밖 변경은
0바이트임을 검사했다. 전체 ROM은 정상 빌드됐다. emucap 실행 검증은 사용자
요청에 따라 생략했다.

## 글상자 (실측)

- 전각 8자/줄, 6줄/화면
- **넘치면 잘리지 않고 다음 장으로 넘어간다**
- 줄바꿈은 **글자 수가 아니라 폭 기준** — 반각은 24자/줄
- **반각 공백(U+0020)은 정상 출력된다** (~5px vs 전각 ~11px). 띄어쓰기는 이걸
  쓴다. 반각 *영숫자*만 두부(네모)로 나온다.

주의: 이 실측은 8자짜리 말풍선 하나에 대한 것이다. 12자짜리 상자는 재지 않았다.

## 가짜 레코드 11개

셀프포인터 판정을 `+24` 고정에서 `24..256` 범위로 푼 덕에 놓치던 870개
레코드(40%)를 찾았지만, 그 대가로 **압축 태그처럼 보이는 쓰레기도 몇 개
걸렸다.** 906바이트 블록이 3MB로 풀린다고 주장한다 — LZ77도 허프만도 그런
압축비는 낼 수 없다.

빌더가 헤더 선언 크기와 실제 해제 크기를 대조해 거른다. 이걸 안 걸렀을 때
증상은 "여유 공간 부족: 2,103,308바이트 더 필요"였다. 정상 레코드만 계산하면
증가분은 **5,863바이트**로 256KB 여유에 넉넉히 들어간다.

압축 태그 분포로도 드러난다 — `huffman0`(28) `huffman1`(10) `huffman3`(2)은
존재하지 않는 형식이다. 진짜는 `lz77`(209) `huffman4`(64)뿐이다.

## 재삽입 불가 레코드 29개

`03 00` 메시지 스트림이 없어 텍스트 조각 탐색으로만 뽑힌 레코드다. 나오는 것은
`遺の` `ゑ引` 같은 2글자 노이즈이고 되돌려 넣을 곳도 없다. 번역기가
`reinsertable: False` 레코드를 건너뛴다. (모델이 `ゑ引`에 대고 "번역할 원문을
입력해 주세요"라고 답한 것이 단서였다.)

## 남은 작업

1. **메인 메뉴 UI 결함 해결** — emucap 관리형 Mesen에서 새 ROM을 깨끗하게
   부팅하면 타이틀과 첫 메뉴까지 정상 진입하고 `게임으로 논다뇨`가 올바른 한글과
   붙임 말버릇으로 표시된다. 그러나 원본의 세 번째 메뉴 항목 `つづきをあそぶ`가
   패치판에서는 `?`로 깨진다. 원본 비교 결과 네 번째 잠금 항목만 `?`인 것이
   정상이라, 미번역 비압축 UI 문자열의 일본어 글리프가 한글용으로 징발된 충돌로
   확정했다. 일본어 이미지 메뉴도 남아 있다. 증거:
   `evidence/private/runtime_new_continue.png`,
   `evidence/private/runtime_menu_stuck.png`,
   `evidence/private/runtime_original_compare.png`. 패치판 emucap run:
   `01M0YPPMRDZYZAQ115KZE4TTAQ`, ROM SHA-1:
   `0059140A2571175CDBD7B58EB787D4A8AAC5ADF3`. 원본 비교 run:
   `01M0YQHS37JM11C1DY14EZV93K`, 원본 ROM SHA-1:
   `177A4D01A88A512CBC8DC5927B55999169359BBC`.
2. **`ui_strings.json` 393개 번역** — 비압축 문자열. UI·크레딧·성우명·상점명·
   스테이지명. 재삽입 경로도 아직 안 만들었다(고정 슬롯이라 길이 제약 있음).
   `1000万円` 같은 반각 숫자를 쓰는 항목이 있는데 **해당 화면을 본 적이 없어
   렌더 경로가 다를 가능성**이 있다.
3. **자막·가사 이미지 350장 ROM 재삽입** — 160×16 사전 렌더 이미지. ROM
   `0x09F378`–`0x0C6424`. 번역 및 `assets/captions_kr/` 재작화는 350장 모두
   완료했으며, `PARTY☆NIGHT` 가사 044~085도 사용자 제공 번역을 기준으로
   반영했다. 렌더 폭 초과는 0장이다.
4. **6줄 넘는 번역 50건** — 넘어가긴 하지만 한 장 더 넘겨야 한다.
5. **이미지 내 문자** — 타이틀 로고, 메뉴 버튼 그림, 미니게임 UI. 재작화 필요.

### 실기 검증 주의

기존 `analysis/savestate_*.mss`는 이전 ROM의 RAM·VRAM과 해제된 텍스트까지
복원하므로 현재 빌드의 텍스트 소비 증거로 사용할 수 없다. 실제로
`savestate_progress.mss`를 불러오면 이전 일본어 대사가 보였다. 새 빌드는 반드시
reset 후 자연 실행 경로로 검증하거나, 현재 ROM에서 새 저장 상태를 만들어야 한다.

## 도구

| 파일 | 역할 |
|---|---|
| `gba_text_extract.py` | 레코드 순회·압축 해제·텍스트 추출 |
| `gba_message_stream.py` | `03 00 <len>` 스트림 파싱/재작성 (무변경 재빌드 284/284 바이트 일치) |
| `gba_lz77_encode.py` | LZ77 인코더 (`MIN_DISP=2`, VRAM 안전) |
| `gba_huffman_encode.py` | 4/8비트 허프만 (BFS 트리, 짝수 주소, 6비트 자식 오프셋) |
| `gba_font_patch.py` | 한자 셀 징발 + 한글 글리프 렌더 |
| `gba_image_extract.py` | MBM → PNG |
| `gba_translate.py` | 로컬 모델 번역 + 검증 |
| `gba_rewrap_cache.py` | 캐시 레이아웃 재계산 (재번역 없이) |
| `gba_build.py` | **단일 빌드 진입점** |

## 바로잡은 오해

지금까지 확신했다가 틀린 것들. 같은 함정에 다시 빠지지 않도록 남긴다.

- **"SJIS 문자 사이에 제어 바이트가 끼어 있다"** — 아니다. LZ77 플래그·역참조
  바이트를 텍스트로 잘못 읽은 것이다. 9~14차 패스에서 3글자 파편 수천 개를 만들었다.
- **"오프닝 말풍선은 이미지다"** — 아니다. 순차적 타일 인덱스는 비례폭 소프트웨어
  렌더러도 만든다. VRAM 쓰기 브레이크포인트로 1bpp 확장기를 찾아 반증했다.
- **"폰트가 ROM에 없다"** — 4bpp로 찾고 있었다. 1bpp다.
- **"가사는 게임에 없다"** — 있다. 사전 렌더 이미지 350장이다.
- **"게임이 압축 태그로 분기한다"** — 안 한다. 통제된 이분 탐색으로 증명했다.
  허프만 자리에 유효한 LZ77 블록을 넣으면 멈추고, 원본 블록을 그대로 옮기면
  동작한다. 그래서 **레코드마다 원래 형식으로 다시 넣어야 한다.**
- **`make_wrap_probe.py`의 판정** — 내가 재지도 않은 값(`--observed-latin 8`)을
  넣어서 나온 결과였다. 실제로 픽셀을 세어 보니 반각 24자/줄, 폭 기준이었다.
  그 도구는 "넘치면 잘린다"고 했지만 관찰 결과는 다음 장으로 넘어간다.
- **`verify`가 처음 보고한 76곳** — 대부분 오탐이었다. 내 줄바꿈이 `점장 카드`를
  `점장`/`카드`로 쪼갠 것이다. 개행을 공백으로 이어 재검사하니 3항목/9곳으로
  줄었다. 단, 단어 중간에서 접힌 경우(`게이머즈를`→`게이`/`머즈를`)는 공백으로
  이으면 여전히 오탐이다.

## 에뮬레이터 (emucap / Mesen2)

세이브스테이트가 있으니 오프닝을 반복할 필요 없다.

- `analysis/savestate_progress.mss` — 오프닝 직후
- `analysis/savestate_shop_gameplay.mss` — 상점 플레이 화면

**주의 2가지:**

- `save_state`/`load_state`는 메인 CPU 명령어 경계 정지에서만 동작한다.
  `pause()` 후 `step(count=1, unit="instructions")`를 한 번 밟고 호출할 것.
- **`reset()`은 브레이크포인트를 전부 지운다.** 반드시 리셋 *후에* 걸 것.

## 2026-08-28 — 이름 입력/선택지 버튼 6개 완료

mGBA 상태의 VRAM/OAM 오프셋을 바로잡아 남은 버튼을 재조사했다. 버튼은 런타임
폰트 합성이 아니라 ROM에 그대로 든 32×16 OBJ 4bpp 이미지였다. 기존 예상 5개 외에
`切替`도 발견해 총 6개를 한글화했다.

- `0xE385A4` `決める` → `결정`
- `0xE386A8` `もどる` → `뒤로`
- `0xE387AC` `切替` → `전환`
- `0xE388B0` `カナ` → `전환`
- `0xE389B4` `いいえ` → `아니요`
- `0xE393DC` `はい` → `예`

새 빌드: `build/digicomm_nyo_kr_name13.gba`. `name12`에서 확인된 6개 자산 범위
밖의 변경은 0바이트다. 패치 ROM에서 다시 추출한 검수 접촉시트는
`analysis/buttons/name13_rom/contact_sheet.png`에 있다.

## 백업

| 파일 | 시점 |
|---|---|
| `cache/translation_cache.wrapped8.bak.json` | 8자 고정 줄바꿈 시절 |
| `cache/translation_cache.rewrapped.bak.json` | 레이아웃 복구 직후, 조각 재번역 전 |

## 2026-08-28 — 일본어 이미지 누락 재감사 및 ROM 재삽입 완료

`assets/images`와 비자막 이미지 감사 기록을 선별 매니페스트와 다시 대조했다.
기존 감사에서는 일본어로 판정됐지만 수동 선별 단계에서 빠진 다음 2개를 추가했다.

- `0ab7630_240x160.png`: `対戦相手を待っています` → `대전 상대를 / 기다리고 있습니다`
- `0b0dfec_240x160.png`: `敗北` → `패배`

`0ab89e0_32x64.png`, `0ab8be0_32x64.png`, `0ab8de4_32x64.png`의
`大セール` 깃발 3개는 사용자 방침에 따라 번역하지 않고 원본을 유지한다.

두 동전 아이콘 `0ab91a0`, `0ab9270`은 확대 확인 결과 일본어가 아니라 광택 픽셀이라
번역 대상에서 제외했다. 감사 응답에 오탐으로 언급된 음악 아이콘·캐릭터 그림·배경도
다시 확인해 제외했다. 회사 로고 2장은 기존 방침대로 보존했다.

최종 선별 매니페스트는 113개이며, 회사 로고 2개를 뺀 번역 PNG 111개를 모두
`build/digicomm_nyo_kr_name16.gba`에 재삽입했다. 72개는 원래 스트림 안에 들어갔고,
39개는 원본 MCM 코덱과 섹션 구조를 유지한 채 안전 영역으로 재배치했다. 압축 증가로
16 MiB 경계를 26,408바이트 넘는 데이터는 GBA의 32 MiB Game Pak 주소 공간에
배치했다.

- 역추출 픽셀 완전 일치: **111/111**
- 보고서: `analysis/image_reinsert_name16.json`
- 누락 2개 비교: `analysis/image_reinsert/missing_displays_compare.png`
- ROM SHA-256: `8842C41CA6B831708CBFC76A26B2A5598714FC9D91F031CF93679BD6D90F8551`

`gba_build.py`에도 이 단계를 연결해 원본 ROM부터의 깨끗한 통합 빌드를 별도로
실행했다. 메시지 3,977줄 빌드 뒤 이미지 111장을 다시 넣고 111/111 역검증까지
성공했으므로, 이후 재빌드에서도 이미지 단계가 빠지지 않는다.

### 2026-08-28 런타임 재검증 및 최종 이미지 삽입 방식 정정

위 `name16`의 32 MiB 확장/Huffman 재압축 방식은 정적 역추출 검증만 통과했을 뿐,
실행 중 이미지가 깨지고 메인 메뉴 부근에서 `FEE0FEE0`로 분기하는 결함이 있었다.
원본 이미지까지 같은 Huffman 인코더로 재압축한 대조 실험에서도 깨졌고, 16 MiB
밖으로 옮긴 raw/LZ77 데이터도 게임이 읽지 못했다. 따라서 위 단락의 배포 방식은
폐기한다.

실행 검증된 방식은 다음과 같다.

- MCM 이미지 섹션 코덱을 Huffman(3)에서 LZ77(2)로 변경한다.
- 압축 결과가 자기 원래 슬롯에 들어가는 섹션은 그 자리에 둔다.
- 넘치는 섹션만 ROM 16 MiB 안의 검증된 미사용 꼬리 영역에 둔다.
- 다른 이미지의 빈 슬롯이나 추정 패딩은 재사용하지 않는다.
- ROM 크기는 16 MiB 그대로 유지한다.

최종 후보 `build/digicomm_nyo_kr_name19.gba`는 111개 이미지를 모두 역추출해
픽셀 완전 일치(111/111)했고, 60개 이미지는 전부 제자리, 51개 이미지는 일부
섹션을 내부 꼬리로 옮겼다. 꼬리 사용량은 89,260바이트다. emucap/Mesen 깨끗한
부팅에서도 백업 메모리 초기화 화면과 한글 오프닝 이미지·대사가 정상 표시됐다.
보고서는 `analysis/image_reinsert_name19.json`이다.

빌드 명령을 수정한 뒤 같은 `name13` 입력에서 다시 만든 최종본은
`build/digicomm_nyo_kr_name20.gba`이며, `name19`와 SHA-256이 완전히 같다
(`96F1DD393CED02686946E4ACDF783D8BC0D09300AFA9B127B900DB06237BAC95`).
따라서 위 런타임 검증은 `name20`에도 그대로 적용된다. 최종 재삽입 보고서는
`analysis/image_reinsert_name20.json`이다.

추가 런타임 진행 검증에서 타이틀/메뉴 구간을 통과해 한글 이름 입력 화면과
그 이후 한글 오프닝 대사까지 진행했다. 연결은 계속 살아 있었고 디버거 이벤트는
0건이며 `FEE0FEE0` 비정상 분기는 재현되지 않았다. 증거 화면은
`evidence/private/name19_progress_32a.png`와
`evidence/private/name19_after_name_start.png`다. `name20`은 `name19`와 완전히
동일한 ROM이므로 같은 결과다.

추가 누락으로 발견된 번역본 두 장은 빌드 입력 폴더에 보존한다.

- `assets/image_extraction/japanese_images/translated_png/0ab7630_240x160.png`
  — `대전 상대를 / 기다리고 있습니다`
- `assets/image_extraction/japanese_images/translated_png/0b0dfec_240x160.png`
  — `패배`

`gba_build.py`도 반드시 `--lz77-relocation --internal-tail` 옵션으로 이미지 재삽입을
호출하도록 수정했다.

## 2026-08-28 (2) — 메인 메뉴 정지 재발: 원인과 근본 수정

`name28`에서 메인 메뉴(`게임으로 논다뇨`) 이동·선택 시 정지가 재발했다.
정적 감사로 원인이 확정됐다. **이미지 문제가 아니라 고정 슬롯 수정의 유실이다.**

원본 ROM과 `name28`의 재삽입 가능 레코드를 전수 대조한 결과:

- 해제 크기가 달라진 레코드 **272개** (예: `0xd19e74` 1736→1560, `0xd1a2ac` 1736→1554)
- 메시지 슬롯 헤더/길이가 어긋난 곳 7개 (`0xd2e164`, `0xd2ea18`)

즉 `name28`은 2026-08-27에 고치기 전의 **가변 길이 재삽입** 상태 그대로였다.
같은 감사를 `digicomm_nyo_kr_fixed.gba`에 돌리면 크기 불일치 0, 슬롯 불일치 0,
메시지 밖 변경 0바이트로 완전히 통과한다.

### 왜 되돌아갔나

이름 입력 계열 패치를 **빌드 파이프라인 밖에서 손으로** 적용했기 때문이다.
`name8b` = `digicomm_nyo_kr.gba`(08-27 01:25, 고정 슬롯 수정 **이전**) + 이름입력
패치였고, `name9 → name10 → name12 → name13`이 그 위에 쌓였다. 고정 슬롯 빌드
`digicomm_nyo_kr_fixed.gba`는 그날 18:32에 따로 만들어졌지만 이 사슬에는 들어가지
못했다. 이후 `name14`~`name28`은 전부 `name13`을 원본으로 이미지만 갈아 끼웠으므로
정지 원인을 그대로 물려받았다. `name19`/`name20`의 "런타임 검증 통과"도 타이틀·이름
입력 구간까지만 봤기 때문에 이 결함을 잡지 못했다.

`gba_build.py` 머리말이 경고하던 바로 그 실패다 — **파이프라인 밖 단계는 조용히
빠진다.**

### 수정

`gba_build.py`가 메시지·폰트 빌드 뒤, 이미지 재삽입 앞에서 다음 5단계를 직접
실행한다. 더 이상 손으로 ROM을 이어 붙이지 않는다.

1. `gba_nameentry_hangul.py` — 이름 입력 그리드 표
2. `gba_nameentry_font8.py` — 8×8 전용 폰트
3. `gba_nameentry_page2.py` — 「カナ」 2페이지 변환표
4. `gba_fix_name_suffix.py` — 「$0」しゃん… → 「$0」씨…
5. `gba_patch_rom_buttons.py` — 32×16 OBJ 버튼 6개

고정 슬롯 모드에서는 메시지 아레나가 더 커져 기존 이름입력 고정 주소와 겹쳤다.
겹치면 조용히 덮어쓰는 대신 빌드가 실패하도록 아레나 상한을 두고, 고정 배치를
위로 옮겼다.

| 대상 | 이전 | 현재 |
|---|---|---|
| 메시지 아레나 | `0xFC0000`–`0x1000000` | `0xFC0000`–`0xFDC000` (89,116 / 114,688 사용) |
| 8×8 폰트 | `0xFD4000` | `0xFDC000` |
| 2페이지 변환표 | `0xFD6000` | `0xFDE000` |
| 이름 접미 블록 | `0xFD7000` | `0xFDF000` |
| 이미지 꼬리 | `0xFD7448`~ | `0xFDF448`~ (44,716바이트) |

타이틀 로고 `0b36c60_240x160.png`는 원래 정렬로 되돌렸다. 오른쪽 2px·아래 1px
이동은 Huffman 재압축본을 원래 슬롯에 넣으려던 임시방편인데, 런타임 검증된
방식(코덱 3→2, LZ77)에서는 어차피 재배치되므로 아무 이득이 없다. 밀어 둔 판은
`analysis/title_logo_shifted_workaround.png`에 남겼다.

### 새 빌드

`build/digicomm_nyo_kr_name30.gba` (16,777,216바이트)
SHA-256: `A9D033D3F2CF7537D4CB37ADAC9358F2A9AF8BB8B81ED97DE37F202136E11088`

검증:

- 재삽입 가능 레코드 273개 / 번역 메시지 3,977개 — 해제 크기 불일치 **0**,
  슬롯 헤더·길이 불일치 **0**
- 메시지 밖 변경은 `0xd303fc`의 `0x46A`–`0x471` 8바이트뿐 — 의도한 이름 접미
  패치이며 슬롯 길이는 그대로다
- 이미지 111장 역추출 픽셀 완전 일치 (111/111), 코덱 3→2, 제자리 64 / 재배치 47
- 버튼 6개 256바이트 범위와 이름 입력 그리드 표는 `name13`과 바이트 동일,
  8×8 폰트 371자도 그대로(주소만 이동)

### `name21`~`name28`에 대해

전부 `name13` 기반이므로 같은 결함을 갖고 있다. 이 구간에서 "타이틀 로고가
크래시 원인"이라고 좁힌 결론은 재검토가 필요하다 — `name26`/`name28`은 폐기된
Huffman 재압축·재배치 경로(코덱 3 유지)를 썼고, 그 경로는 08-28 기록에서 이미
런타임에 깨진다고 확인된 방식이다.


## 2026-08-28 (3) — 이미지 재배치가 메인 메뉴를 죽인다 (실행 검증)

에뮬레이터(emucap/Mesen)로 직접 끝까지 몰아본 결과, 앞 절의 정적 수정만으로는
부족했다. **원인은 두 개였고 두 번째가 진짜 이미지 문제다.**

### 기존 "런타임 검증됨" 기록은 틀렸다

`name20`을 깨끗하게 부팅해 오프닝 → 선택지 → 이름 입력까지 진행한 뒤 메인 메뉴로
넘어가는 순간 CPU가 `PC = 0x03A0F988`(존재하지 않는 주소)로 튀어 죽었다. 화면은
별 배경만 남고 검게 꺼진다. r1~r9·r14가 전부 `0x03A0F944`로 채워진, 전형적인
쓰레기 워드 로드다.

즉 `name19`/`name20`을 "런타임 검증 통과"로 적은 기록은 **메인 메뉴 직전까지만
본 것**이다. 이후 `name21`~`name30`이 전부 이 위에 쌓였다. 사용자가 본
`FEE0FEE0`도 같은 고장이다 — `FE E0`은 LZ77 역참조 토큰(길이 18, 거리 3809)
바이트라, 압축 이미지 스트림을 주소로 읽어 분기했다는 뜻이다.

### 갈라 본 방법

1. `name31` = 고정 슬롯 메시지 + 이름입력·버튼 패치, **이미지 재삽입 없음**.
   오프닝 → 예/아니요 선택지 → 이름 입력(「가」씨…) → 메인 메뉴 → 위/아래 이동 →
   선택까지 전부 정상. 메시지 쪽 수정은 옳다.
2. `name20` = 같은 지점에서 사망(위).

따라서 결함은 이미지 재삽입 경로에 있고, 메시지·폰트·이름입력과는 무관하다.

### 무엇이 문제인가

MCM 섹션을 **원래 할당 밖으로 옮기는 것** 자체가 문제다. 헤더에 섹션마다 포인터가
있어도 게임은 이미지를 통째로 읽을 때 압축 블록의 소비 길이를 따라 물리적으로
이어 읽는다. 한 섹션이라도 원래 자리를 벗어나면 그다음부터 엉뚱한 바이트를 블록
헤더로 해석한다.

`gba_image_reinsert.py`에 `--in-place-only`를 넣었다. 섹션 체인 전체가 원래 할당
안에 그대로 들어가는 이미지만 넣고, 한 바이트라도 넘치는 이미지는 손대지 않는다.
`gba_build.py`는 `--images-in-place-only`로 이 모드를 켠다.

### 검증된 빌드

`build/digicomm_nyo_kr_name32.gba` (16,777,216바이트)
SHA-256: `D266E556230C9F31B78D1EEB8B815CDE82D7EC3B9D698F59E3ACAD12D0AABC98`

emucap/Mesen 깨끗한 부팅에서 직접 확인한 구간:

- 오프닝 대사 한글 출력
- 첫 만남 선택지 `예`/`아니요` — 선택 후 정상 진행
- 한글 이름 입력 그리드·버튼(전환/뒤로/결정), 입력 후 `「가」씨…`
- 타이틀 화면
- **메인 메뉴 진입, 위/아래 이동, 항목 선택** — 정지·크래시 없음
- 하위 메뉴 `처음부터 시작한다뇨` 선택 후 상점 화면(`어서오세요게마~！`)까지 진행

정적 검증도 그대로 통과한다 — 레코드 273개/메시지 3,977개, 크기·슬롯 불일치 0,
메시지 밖 변경은 의도한 이름 접미 8바이트뿐. 이미지 81장 역추출 픽셀 완전 일치.

### 미번역으로 남은 이미지 30장

원래 슬롯에 안 들어가서 일본어 원본을 유지한다. 타이틀 로고와 메인 메뉴 항목 그림이
여기 포함된다.

```
00a29c8_96x16     0ab43a4_240x160   0aeb6dc_32x32
00fd114_16x16     0ab5c4c_240x160   0aece1c_32x32
00fd218_16x16     0ab7630_240x160   0aed7e8_64x32
00fd2ec_16x16     0ab9718_64x32     0b36c60_240x160  (타이틀 로고)
00fd5b4_16x16     0acbbac_240x160   0b452a4_96x320
00fd834_16x16     0ad093c_240x160
00fdc20_16x16     0ae9be4_64x32
00fdca0_16x16
00fdd18_16x16     그 외 16x16 아이콘 다수
00fdd98_16x16
00fe08c_16x16  00fe21c_16x16  00fe328_16x16  00fe42c_16x16
00fe638_16x16  00fe744_16x16  00fe7c0_16x16  00fe9cc_16x16
```

### 다음에 이 30장을 넣으려면

재배치 대신 **압축을 더 줄여 원래 슬롯에 넣는 것**이 유일하게 검증된 방향이다.
`gba_lz77_encode.compress_optimal`(동적 계획 파서)이 이미 있고 코덱 2 경로에서
쓰면 스트림이 작아진다. 다만 최적 압축본은 아직 실행 검증을 못 받았으므로,
켤 때는 반드시 메인 메뉴 진입까지 다시 확인할 것. 현재 코덱 2 경로는 검증된
그리디 인코더를 쓴다.

폐기: "섹션을 ROM 꼬리로 옮겨도 된다"는 결론. `name14`~`name30`의 모든 변형
(32 MiB 확장, Huffman 유지, LZ77 재배치, 체인 패킹)이 전부 같은 지점에서 죽었다.

### 최종 — 재배치 없이 105장 삽입 (`name33`)

재배치가 원인이면 해법은 "옮기지 않고 자리에 들어가게 만드는 것"이다. 세 가지를
넣어 81장 → **105장**으로 올렸다.

1. **코덱을 바꾸지 않는다.** 이 이미지들은 원본이 Huffman인데 LZ77로 바꾼 것 자체가
   손해였다(`0ad093c`: 원본 1,908 → LZ77 2,908). 자리에 안 들어가면 원본 코덱으로,
   **원본의 Huffman 트리를 그대로 재사용해** 다시 인코딩한다. 비트스트림만 바뀐다.
   → 23장 중 15장 회수.
2. **넘칠 때만 최적 LZ77.** `compress_optimal`(동적 계획 파서)을 자리에 안 들어가는
   섹션에만 쓴다. 검증된 그리디 인코더가 기본이다.
3. **안 바뀐 셀은 원래 타일 인덱스를 유지한다.** 기존 인코더는 모든 셀을 셀 순서로
   새 타일 배열에 다시 채워, 그림이 안 바뀐 곳까지 페이로드를 통째로 뒤섞었다.
   원본과 닮지 않은 바이트열은 압축이 나쁘다. 이제 원본에 이미 있는 타일이면 그
   인덱스를 그대로 쓰고, 번역이 실제로 다시 그린 셀에만 새 슬롯을 쓴다. 뒤집기
   후보 중에서도 원본에 있는 타일을 우선 고른다.

타이틀 로고 `0b36c60`은 원래 정렬로는 44바이트가 모자라, 오른쪽 2px·아래 1px 이동판을
채택했다(5,868 ≤ 5,932). `00a29c8`도 1px 왼쪽 이동으로 들어간다. 원래 정렬본은
`analysis/title_logo_pre_shift.png`, `analysis/00a29c8_pre_shift.png`에 있다.

**`build/digicomm_nyo_kr_name33.gba`** (16,777,216바이트)
SHA-256: `8D6F842B26F058C2BA9C39779B0FABCA998C0AFB8214ECF0DC6164800D0B63FE`

- 이미지 105/111, 역추출 픽셀 완전 일치, **꼬리 사용 0바이트 — 재배치 없음**
- 레코드 273개 / 메시지 3,977개, 크기·슬롯 불일치 0
- emucap/Mesen 실행: 오프닝 → 선택지 → 이름 입력 → **한글 타이틀 로고** →
  메인 메뉴 진입·이동·선택 → 상점 화면까지 정상. 디버거 이벤트 0건.

#### 아직 일본어인 6장

| 파일 | 여유 | 필요 | 부족 |
|---|---|---|---|
| `0ad093c_240x160` | 1,908 | 2,576 | 668 |
| `0acbbac_240x160` | 4,128 | 4,972 | 844 |
| `0ab43a4_240x160` | 5,020 | 5,396 | 376 |
| `0ab5c4c_240x160` | 5,336 | 5,560 | 224 |
| `0ae9be4_64x32` | 1,012 | 1,028 | 16 |
| `00fd218_16x16` | 40 | 56 | 16 |

체인 끝 뒤에 남는 여유는 0바이트(`0ae9be4` 128, `00a29c8` 32 뿐)로, ROM이 빽빽하다.
남은 수단은 **그림을 덜 복잡하게 다시 그리는 것**뿐이다. 한글 획이 원래 일본어보다
잔무늬가 많아 압축이 커진다. `0ae9be4`와 `00fd218`은 16바이트만 줄이면 된다.
1~3px 이동 자동 탐색은 이미 돌렸고 이 6장은 어떤 이동으로도 못 맞춘다.


## 2026-08-28 (4) — 미니게임 프리징: MCM 섹션 체인 truncation (실행 추적)

`name33`에서 미니게임 설명 끝나고 시작하면 멈추는 문제를 emucap/Mesen으로 재현해
원인을 확정했다. 정지 지점은 `PC=0x080479BE`의 `B .`(0xE7FE) 무한 루프이며, 그
직전에 게임이 `0x08145AAC`의 문자열 **`unknown script directive '%d'.`** 로
패닉한다. 디렉티브 값은 32였다.

디스패처(`0x080478D8`)를 역어셈블해 구조를 얻었다.

```
r7 = ctx
base = [ctx+0x44]        ; 해제된 스크립트 버퍼
pc   = u16 [ctx+0x5A]    ; 하프워드 단위 PC (읽고 +1)
dir  = ldrsh [base + pc*2]
if dir > 0x26 -> 0x080479B4 (패닉)
jump handler_table[dir]  ; 0x08047914, 40엔트리, 미정의는 전부 패닉
```

멈춘 시점의 ctx=`0x02019218`, base=`0x020091C0`, pc=90 → 오프셋 180을 읽었다.
그 버퍼를 ROM 레코드와 대조하니 `0xD704FC`이고, 스크립트는 레코드 오프셋
`0xF64`에서 시작한다. 즉 `0xF64+180 = 4120`으로 **레코드 4,096바이트를 넘어섰다.**

### 원인

`0xD704FC`의 MCM 헤더는 원본이 `size=0x2130(8,496) chunk=0x1000 count=3`이다.
빌더는 자기 포인터(`tag+20`)가 가리키는 **첫 섹션만** 풀어서 다시 넣고,
마지막에 `struct.pack_into("<I", rom, tag_off + 4, len(rebuilt))` 로
**size를 0x2130 → 0x1000 으로 덮어썼다.** 나머지 두 섹션이 통째로 사라져
스크립트가 잘렸고, 해석기가 끝을 넘어가 쓰레기를 디렉티브로 읽었다.

번역 대상 273개 중 **18개가 다중 섹션**이다. 전부 같은 방식으로 잘려 있었다.

```
0xd1c644(3) 0xd1f4f0(2) 0xd213a8(2) 0xd22334(2) 0xd235a4(2) 0xd26aec(2)
0xd277c8(2) 0xd28460(2) 0xd2a6ec(2) 0xd31d54(4) 0xd60830(2) 0xd624d8(2)
0xd704fc(3) 0xd7298c(2) 0xd74a4c(2) 0xd76924(2) 0xd786f4(2) 0xd7a434(2)
```

`0xd7298c`는 STATUS가 "300인 선착순 미니게임"으로 적어 둔 그 레코드다.

### 수정

`gba_build.py`가 MCM 체인 전체를 다룬다.

- 헤더의 `size/chunk/count`를 읽고 **모든 섹션 포인터**를 따라 풀어 이어 붙인다
- 메시지를 다시 쓴 뒤 `chunk` 단위로 다시 잘라 섹션별로 재압축한다
- 이미지와 같은 규칙으로 배치한다 — 체인 전체가 원래 할당에 들어가면 첫 포인터
  자리에서 연속으로 다시 쌓고, 넘치면 체인 전체를 아레나로 옮기며 `count`개
  포인터를 모두 갱신한다
- **`size` 필드는 절대 건드리지 않는다.** 고정 슬롯이므로 해제 크기가 같고,
  달라지면 빌드를 실패시킨다

감사도 체인 기준으로 다시 만들었다(`cache/audit_fixed_slots.py`의 확장판).
`name34`: 레코드 273 / 메시지 3,977, MCM 헤더 변경 0, 체인 크기 불일치 0,
슬롯 불일치 0, 메시지 밖 변경은 의도한 이름 접미 8바이트뿐.

**`build/digicomm_nyo_kr_name34.gba`**
SHA-256: `637E39BAC2AF3BA44BC6801AAE15042E28EFC72862A8345AB0F12AFE0AF6F527`

실행 검증: 오프닝 → 선택지 → 이름 입력 → 한글 타이틀 → 메인 메뉴 → 상점 →
**미니게임 튜토리얼 설명 → 시작**까지 통과해 실제 보드 화면에서 플레이가 이어졌다.
`name33`이 멈추던 지점이다.

### 파생 사실 — 2~4번째 섹션의 텍스트는 아직 추출조차 안 됐다

추출기도 첫 섹션만 읽었으므로, 위 18개 레코드의 뒤쪽 섹션에 있는 대사는
`messages.json`에 아예 없다. 실행 화면에서 튜토리얼의 `ここまでの説明はわかったゲマ?`
가 일본어로 남아 있는 것이 그 증거다. `gba_text_extract.py`도 체인 전체를 읽도록
고쳐 재추출·번역해야 한다. (미착수)

## 2026-08-28 (5) — 메인 메뉴 라벨은 MBM이 아니라 원시 OBJ 스프라이트

`ゲームであそぶ` 같은 메뉴 항목이 추출 목록에 없던 이유를 찾았다. **MCM 컨테이너가
아니다.** ROM에 압축 없이 들어 있는 4bpp OBJ 타일이라 MBM 순회기가 보지 못한다.

메인 메뉴에서 OAM을 뜬 결과:

```
obj122 x=64 y=68 64x32 tile=960 pal=15   ← ゲームであそぶ 오른쪽 절반
obj123 x= 0 y=68 64x32 tile=992 pal=15   ← 왼쪽 절반
obj124..127                              ← 잠금 항목의 회색 타원(?) pal=14
```

VRAM 타일을 ROM에서 그대로 찾으니 `0xF91360`(왼쪽) / `0xF91764`(오른쪽)이다.
간격 `0x404` = 64×32 타일 1,024바이트 + 구분자 4바이트. 이미 한글화한 32×16 버튼
6개가 `0x104` 간격이었던 것과 같은 형식이다.

표를 그 보폭으로 훑어 **41장**을 추출했다(라벨 하나가 좌우 2장). `0xF8CB18`부터
`0xF96BB8`까지이며, 아래를 포함한다.

`アルバム` `はじめからあそぶ` `カード…お店` `コレクション` `ともだちとあそぶ`
`つづきをあそぶ` `デパック` `対戦の…をつくる` `ゲームであそぶ` `めいし`
`めいしこうかん` `めいしをみる` `おもいで` `ステージセレクト` `店長ランキング`
`お店をみる`

- 도구: `tools/gba_extract_obj_labels.py`
- 산출: `assets/obj_labels/` (PNG 41장 + `contact_sheet.png` + `manifest.json`)
- 팔레트는 스프라이트에 없으므로 메뉴 화면 PRAM 덤프(`assets/obj_labels/menu_pram.bin`,
  뱅크 15)를 쓴다

**재삽입은 크기 위험이 없다.** 비압축 고정 크기 블록이라 버튼 6개와 똑같이 제자리
덮어쓰기면 된다. 번역·재작화는 미착수.

타이틀 화면의 `スタート`는 이 표에 없다. OAM이 전부 비활성이었으므로 BG 레이어이며
별도 추적이 필요하다. (미착수)


## 2026-08-28 (6) — 잘려 있던 섹션의 미추출 대사 재추출

`gba_text_extract.py`도 빌더와 같은 결함이 있었다. MCM의 **첫 섹션만** 풀어서
읽었으므로, 다중 섹션 레코드의 두 번째 이후 섹션에 있는 대사는 `messages.json`에
아예 들어온 적이 없다.

포인터 표가 `tag+20`에서 시작하므로 첫 섹션은 정확히 `20 + count*4` 바이트 뒤에
온다 — 자기 포인터가 `+24`뿐 아니라 `+28`, `+32`, `+36`에서도 발견되던 이유가
이것이다. 이 관계를 검사로 삼아 `count`를 신뢰하고 모든 섹션 포인터를 따라간다.

- 레코드에 `section_count`, `chunk_size`를 기록한다
- `decompressed_size`는 이제 **체인 전체 크기**다. `gba_build.py`의 위조 레코드
  판정도 헤더의 `size`와 함께 이 값으로 맞췄다

### 결과

| | 이전 | 이후 |
|---|---|---|
| 레코드 | 313 | 313 |
| 메시지 | 4,032 | **4,695** |
| 문자 | — | 87,702 |

새로 나온 663줄은 전부 앞서 잘려 있던 18개 레코드에서 나왔다.

```
0xd1c644  97   0xd31d54  86   0xd704fc  79   0xd624d8  70   0xd213a8  56
0xd28460  37   0xd7a434  36   0xd7298c  35   0xd2a6ec  32   0xd277c8  25
0xd26aec  24   0xd1f4f0  18   0xd74a4c  17   0xd235a4  16   0xd76924  13
0xd786f4  11   0xd22334   9   0xd60830   2
```

기존 번역은 `(mcm_offset, header)` 로 병합했다. **3,996건 이관, 원문 불일치 0건.**
헤더 오프셋은 섹션 0 안의 위치이므로 이어 붙여도 그대로 유효하다.

- 이전 추출본 백업: `cache/messages.first_section_only.bak.json`
- 캐시 백업: `cache/translation_cache.pre_chain.bak.json`

새 663줄은 오프닝 이후 본편·미니게임 튜토리얼 대사다. 예:
`にょにょっ、負け惜しみですか？…`, `ここまでの説明はわかったゲマ?`,
`な、なぞのあやしい力の働きで、ゲーマーズの経営状態が急激に悪化したゲマーっ！`

### 번역·재삽입 완료 (`name36`)

새 663줄을 번역했다. 모델은 기존과 같은 `gemma-4-26b-a4b-it-qat`이다.

| 단계 | 결과 |
|---|---|
| 1차 (재시도 2) | 508 채택 / 35 거부 |
| 2차 (재시도 8) | +1 / 34 거부 |
| 3차 (어미 복구 도입) | +29, **어미 복구 29건** / 5 거부 |
| 4차 (줄바꿈·어미 순서 버그 수정) | +1 / 4 거부 |
| 수기 | 4건 |

**거부 사유는 거의 전부 말끝 어미 누락이었다.** 프롬프트가 줄마다 요구 어미를
명시하는데도 같은 줄이 매번 똑같이 실패한다 — 재시도로 풀리지 않는다. 어미는
번역 판단이 아니라 정해진 접미사이므로, **다른 문제가 하나도 없고 어미만 빠진
경우에 한해** `repair_missing_speech_tic()`이 앞말에 붙여 넣고 재검증한다.
문장부호는 어미 뒤에 남긴다 (`~한다게마！`).

같이 고친 버그: 어미를 붙인 **뒤에** 줄바꿈을 하고 있어서, 줄바꿈이 어미를 다음
줄로 밀면 `must attach`로 거부됐다. 순서를 뒤집었다.

주의: 번역기 로그는 원문을 24자에서 자른다. 그 잘린 문자열을 캐시 키로 쓰면
반영되지 않는다(실제로 2건 겪음). 수기 수정은 반드시 전체 원문을 키로 쓸 것.

### 슬롯 축약 — `tools/gba_fit_slots.py`

번역 후 슬롯 초과가 128건이었다. 기존 `cache/fit_all_message_slots.py`는
**바이트 길이만 보고 채택**해서, 128건 중 48건이 문장을 중간에서 자르거나
(`… 수는 없는 게`) 말끝 어미를 잃었다. 그대로 넣으면 품질이 후퇴한다.

새 도구는 값싼 순서로 시도하고 **`gba_translate.validate`를 통과한 것만** 채택한다.

1. **공백 제거** — 원문 일본어에는 띄어쓰기가 없으므로 무손실이고 공백 1개가
   1바이트다. 이것만으로 **75건** 해결.
2. 이전 축약본 재사용 — 검증을 통과하는 것만. **22건**.
3. 모델 축약 — 검증 통과분만. **22건**.
4. 나머지 **9건은 수기**.

결과: 초과 0건.

**`build/digicomm_nyo_kr_name36.gba`**
SHA-256: `B28DE64BA80B3D5B4C0FE1B536AF0F056DAEF54CB7A1482AA3BB8134254512C9`

- 메시지 **4,640줄** 재삽입 (이전 3,977)
- 체인 감사: MCM 헤더 변경 0, 크기 불일치 0, 슬롯 불일치 0
- 실기: 오프닝 → 이름 입력 → 타이틀 → 메뉴 → 상점 → 보드 →
  **미니게임 튜토리얼이 한글로 출력**(`여기까지 설명은 이해했나 게마?`) → 플레이 지속


## 2026-08-28 (7) — 남은 일본어는 전부 원시 OBJ 스프라이트다

보드 화면에서 OAM/VRAM을 떠서 화면에 남은 일본어의 출처를 전부 확인했다.
**MCM 컨테이너가 하나도 없다.** 전부 ROM에 압축 없이 든 4bpp OBJ 타일이라
`gba_image_extract.py`(MBM 순회기)가 볼 수 없었고, 그래서 추출 목록에 없었다.

| 대상 | 크기 | ROM | 비고 |
|---|---|---|---|
| 메인 메뉴 항목 (`ゲームであそぶ` 등) | 64×32 ×2 | `0xF8CB18`~ | 간격 `0x404`, 41장 추출 완료 |
| 손패 카드 (`しごと よびこみ Lv.1` 등) | 32×32 | `0xE700AC`~ | 간격 `0x204`, 59장 추출 완료 |
| `すすむ` 버튼 | 32×32 | `0xE6FEA8` | |
| 이름 입력 버튼 6개 | 32×16 | `0xE385A4`~ | 간격 `0x104`, **한글화 완료** |
| 아이콘류 (돈·점수·카드 등) | 16×16 / 32×16 | `0xE3D168`, `0xE3D884` 등 | 글자 없음 |

찾는 방법은 매번 같다.

1. 해당 화면에서 `debug dump_memory` 로 vram/oam/pram 을 뜬다
2. OAM에서 활성 스프라이트의 타일 번호·크기·팔레트를 읽는다
3. 그 타일의 VRAM 바이트열을 ROM에서 그대로 찾는다 — 비압축이라 바로 나온다
4. 이웃 항목은 고정 간격(크기 + 4바이트 구분자)으로 이어진다

`tools/gba_extract_obj_labels.py --width/--height/--anchor` 로 어느 표든 뽑는다.
팔레트는 스프라이트에 없으므로 그 화면의 PRAM 덤프를 넘긴다
(`assets/obj_labels/menu_pram.bin`, `board_pram.bin`).

### 산출물

- `assets/obj_labels/` — 메뉴 라벨 41장 + 접촉시트 + manifest
- `assets/obj_labels/cards/` — 카드 59장 + 접촉시트 + manifest

카드 표에서 확인된 것: `ターボ×1`~`×7`(コスト1), `よびこみ`, `カード`,
`おジャマ`, `販売`, `会員`, `ものまね`, `盛りつけ` 등.

### 재삽입은 크기 위험이 없다

비압축 고정 크기 블록이므로 이름 입력 버튼 6개와 똑같이 제자리 덮어쓰기다.
압축·재배치·슬롯 문제가 전혀 없다. 남은 일은 한글 재작화뿐이며,
`gba_patch_rom_buttons.py`가 쓰는 Galmuri 8px 비트맵 폰트를 그대로 쓰면 된다.
(미착수)


## 2026-08-28 (8) — 보드 카드·버튼 한글화 (`name37`)

`tools/gba_patch_obj_cards.py`로 원시 32×32 OBJ 카드 스프라이트에 한글을 그린다.
비압축 고정 크기 블록이라 제자리 덮어쓰기이며, 압축·재배치·슬롯 문제가 없다.

세 가지 그리기 모드가 있다.

- **banner** — 위/아래 단색 띠. 그 영역의 최빈 인덱스를 배경으로 삼아 지우고,
  배경이 밝으면 먹색(5), 어두우면 흰색(15)으로 글자를 쓴다.
- **ribbon** — 카드 이름은 그림 위에 외곽선으로 그려져 있어 **아래 픽셀을 복원할
  수 없다.** 그래서 같은 자리에 흰 리본(테두리 2, 안쪽 15)을 깔고 먹색으로 쓴다.
  원래 일본어도 그 부분 그림을 가리고 있었다.
- **outlined** — `すすむ` 버튼처럼 배경이 투명한 경우. 지우고 먹색 외곽선 +
  흰 글자로 쓴다. 원본도 프레임을 끊고 글자가 더 넓게 나가 있으므로 같은 범위를
  지운다(y=12..20 전폭).

색은 항상 **팔레트 인덱스**로 다룬다. 스프라이트에 팔레트가 없고 보드가 런타임에
뱅크 15를 준다. 화면의 PRAM 덤프는 `assets/obj_labels/board_pram.bin`이다.

폰트는 버튼과 같은 **Galmuri7Bitmap, 크기 10**(이 폰트는 10에서만 열린다).
글자 폭은 한글 1자당 약 7.4px이므로 카드 안쪽 24px에 **3자까지** 들어간다.
아래 띠는 더 좁아 `코스트1`(4자, 26px)이 안 들어가 **`비용1`**로 옮겼다.

### 적용

- 배너: `店長`→**점장**(19장), `ターボ`→**터보**(7장), `しごと`→**업무**(7장),
  `おジャマ`→**방해**(2장), `コスト0/1/2`→**비용0/1/2**
- 이름 리본: `よびこみ`→**호객**, `整とん`→**정돈**, `販売`→**판매**,
  `会員`→**회원**, `カード`→**카드**, `すてふだ`→**버림패**
- `すすむ` 버튼 → **진행**

스펙은 `assets/obj_labels/cards_spec.json`이 소유한다(오프셋·사각형·문구).

**`build/digicomm_nyo_kr_name37.gba`**
SHA-256: `C2B27BA20F2886FF26D00AC00412E98A511EB970AD278810232DCA3E1887E049`

- 변경 5,447바이트, **패치 대상 스프라이트 밖 변경 0바이트**
- 실기: 손패가 `업무 / 호객 / Lv+1`, 네 모서리 버튼이 `진행`으로 한글 출력

### 점장 카드 이름은 손대지 않았다

`店長` 계열 19장의 이름은 5px 가나라 렌더에서 확실히 읽히지 않는다. 스크립트가
확인해 준 것은 `大セール` `さぼり` `暴発` `コスト` `テレビＣＭ` `客いれかえ`
`返品` `ぶんどり` `１００万円` `呪い` `ルーレット` 정도이고, 어느 스프라이트가
어느 이름인지 확정할 수 없다. **틀린 이름을 넣느니 일본어로 두는 편이 낫다** —
카드 효과를 오인하게 만든다. 어느 카드가 무엇인지 확인되면
`cards_spec.json`에 `ribbons` 한 줄씩 추가하면 끝난다.


### 원본/번역 PNG 쌍 (필수 규칙)

처음에 스펙 → ROM 바이트로 곧장 써서 **번역 이미지가 파일로 남지 않았다.**
`japanese_images/png` + `translated_png` 와 같은 규칙을 OBJ 스프라이트에도 적용했다.

```
assets/obj_labels/
  cards/png/              원본 59장
  cards/translated_png/   번역 36장
  cards/compare_original_translated.png   좌=원본 우=번역 대조 시트
  cards_spec.json         오프셋·사각형·문구
  board_pram.bin          보드 화면 팔레트(뱅크 15)
  menu/png/               메뉴 라벨 원본 41장 (번역 미착수)
  menu_pram.bin
```

`gba_patch_obj_cards.py`는 이제

- 패치할 때마다 **원본 PNG를 항상 다시 쓴다** — 쌍이 있어야 검수가 된다
- 그린 결과를 **번역 PNG로 저장**하고, 그 PNG를 다시 인덱스로 읽어 ROM과
  일치하는지 매번 검사한다
- `--from-translated` 로 **PNG만 읽어 재삽입**한다. 즉 그림을 손으로 고치면
  스크립트를 건드리지 않고 그대로 반영된다

PNG만으로 재삽입한 ROM이 그려서 만든 ROM과 바이트 단위로 동일함을 확인했다
(SHA-256 `C2B27BA20F2886FF26D00AC00412E98A511EB970AD278810232DCA3E1887E049`).

PNG는 RGBA이고 색은 런타임 팔레트 뱅크 15 그대로다. 팔레트에 없는 색이 하나라도
있으면 재삽입이 거부된다 — 4bpp 16색 밖으로 나가는 수정을 막는다.


## 2026-08-28 (9) — 메뉴 라벨 한글화, 카드 비용 표기 축소 (`name39`)

### 메뉴 라벨 19개 — `tools/gba_patch_obj_menu.py`

메뉴 항목 하나는 **128×32 그림 한 장이 64×32 스프라이트 두 개**(간격 `0x404`)로
나뉘어 있다. 좌우를 이어 붙여 그리고 다시 쪼개 넣는다.

두 가지 스타일이 섞여 있고, 자동으로 가른다.

- **타원형** — 넓은 타원 위에 글자. 타원은 바깥부터 `7 → 14 → 8` 동심 링이므로,
  원본 실루엣을 침식(erode)해 링을 다시 깔면 그대로 복원된다.
- **글로우형** — 타원이 없고 글자 윤곽을 따라 후광이 붙는다. 이 경우 원본
  실루엣을 쓰면 **옛 일본어 글자 모양의 얼룩**이 남으므로, 새 한글에서 후광을
  다시 만든다(글자 → 어두운 테두리 → 8 → 7, 총 3px).

판별은 실루엣 위·아래 가장자리의 거칠기다. 이웃 열 사이 단차 합이
타원 36~42, 글로우 52~77로 깨끗하게 갈린다(임계 45).

| 원문 | 한국어 | 원문 | 한국어 |
|---|---|---|---|
| アルバム | 앨범 | めいし | 명함 |
| はじめからあそぶ | 처음부터 놀기 | めいしこうかん | 명함 교환 |
| カードとお店 | 카드와 가게 | でんぱでめいしこうかん | 전파로 명함 교환 |
| コレクション | 컬렉션 | めいしをみる | 명함 보기 |
| ともだちとあそぶ | 친구와 놀기 | おもいで | 추억 |
| つづきをあそぶ | 이어서 놀기 | ステージセレクト | 스테이지 선택 |
| デバッグ | 디버그 | 店長ランキング | 점장 랭킹 |
| 対戦の山札をつくる | 대전 덱 만들기 | お店をみる | 가게 보기 |
| ゲームであそぶ | 게임으로 놀기 | | |

### 비용 표기: `비용N` → 작은 `COSTN`

`코스트1`은 4자 26px이라 23px 띠에 안 들어가 `비용1`로 옮겼었다. 사용자 요청대로
**원문 가타카나와 같은 크기의 3×5 도트 폰트**를 도구에 내장해 `COST1`을 19px로
쓴다. 띠가 7행 → 5행으로 줄어든 만큼 **이름 리본을 `y=12..22`에서 `y=10..23`으로
넓혔다.**

### 원본 PNG는 반드시 일본어 ROM에서 뜬다

이미 패치된 ROM을 `--rom`으로 주면 "원본" PNG에 이전 번역이 찍힌다(실제로 한 번
그렇게 나왔다). 두 도구에 `--original-rom`을 넣어 원본 쌍은 항상 손대지 않은
일본어 ROM에서 뜬다.

**`build/digicomm_nyo_kr_name39.gba`**
SHA-256: `8E07A1740C42CA435825C2FFCE23F4B0A5E7E6BD184B8A98C4D129C6D57ADE8E`

- 메시지 감사: 레코드 273 / 메시지 4,640, 헤더·크기·슬롯 불일치 0
- 실기: 메인 메뉴가 `게임으로 놀기`로 출력

### 점장 카드 이름: 10장 번역, 9장 보류

넓어진 리본에 확실히 읽히는 것만 넣었다.

`ものまね`→흉내, `１００万円`→백만원, `客いれかえ`→교체, `大セール`→세일,
`暴発`→폭주, `おやつ`→간식, `浄化`→정화, `さぼり`→태만, `テレビＣＭ`→광고,
`コスト`→코스트

아직 일본어인 9장(`0xE6E270` `0xE6E678` `0xE6E87C` `0xE6EE88` `0xE6F08C`
`0xE6F290` `0xE6F494` `0xE6F89C` `0xE6FCA4`)은 5px 가나를 확대해도 확정이 안 된다.
스크립트에 남은 미배정 이름은 `返品` `ぶんどり` `呪い` `ルーレット` `イベント`
정도다. **어느 스프라이트가 어느 이름인지 확인되면 `cards_spec.json`에
`ribbons` 한 줄이면 끝난다.**
## 2026-08-28 — OBJ 카드 이미지 원본 디자인 보존 재작업 (`name40` 검증본)

사용자 지시에 따라 `assets/obj_labels/cards/translated_png`의 번역 카드 36장을
원본 `cards/png`에서 다시 렌더링했다.

- `１００万円`은 통화 단위를 바꾸지 않고 **`100만엔`**으로 수정했다.
- 원문의 2단 구성처럼 위쪽에 `100`, 아래쪽에 `만엔`을 배치했다.
- 기존의 흰색 사각 리본 방식은 폐기했다. 일본어 글자 면과 바로 맞닿은 외곽선
  픽셀만 제거한 뒤, 원본과 같은 흰 글자 + 남색 1px 외곽선으로 직접 그린다.
- 매번 `cards/png`의 일본어 원본을 소스로 사용하도록
  `gba_patch_obj_cards.py --source-original-pngs`를 추가했다. 이미 번역된 ROM을
  다시 입력해도 이전 리본이나 글자가 누적되지 않는다.
- 결과 36장은 모두 32×32, indexed `P`, transparency index 0이며 런타임
  16색 팔레트 밖 색을 쓰지 않는다.

검수 파일:

- `assets/obj_labels/cards/compare_original_redesign.png` — 좌 원본/우 재작업
- `assets/obj_labels/cards/money_100manen_preview.png` — `100만엔` 20배 확대
- `build/digicomm_nyo_kr_name40.gba` — 카드 변경만 확인하기 위한 검증 ROM
## 2026-08-28 — 카드 배경색 수정 및 OBJ 메뉴 19개 재작업 (`name41`)

### 카드

`점장`과 `COST` 영역을 흰색으로 평탄화하던 배경 판정 오류를 수정했다.
일본어 글자 픽셀만 제거하고 원본 카드의 보라/주황/초록 등 고유 팔레트 배경을
그대로 보존한다. `100만엔` 표기와 32×32 indexed-P/투명 인덱스 0 규격은 유지한다.

### 메뉴

`assets/obj_labels/menu/translated_png` 19개를 원본 128×32 PNG에서 다시 만들었다.

- 기존 Galmuri7 8px 글자를 폐기하고 원문과 비슷한 실제 17px 높이의
  `Galmuri14Bitmap`을 사용한다.
- 타원형 메뉴는 일본어 실루엣을 재사용하지 않고 원본 외곽 실루엣과 항목별
  고유 청색/보라색 팔레트를 측정해 깨끗한 3중 픽셀 링으로 재구성한다.
- 원본 바깥쪽의 작은 별/하이라이트 연결 성분은 복원하고 일본어 잔상은 제거한다.
- 글로우형 메뉴는 흰 글자, 남색 1px 외곽선, 보라색 픽셀 후광을 원문 크기로
  다시 만든다.
- `--source-original-pngs`를 추가해 과거 번역 결과가 누적되지 않게 했다.

검증 결과: 카드 36개 + 메뉴 19개 모두 원본 크기, indexed `P`, transparency 0,
원본 런타임 팔레트 일치.

- 메뉴 대조: `assets/obj_labels/menu/compare_original_redesign.png`
- 카드 배경 대조: `assets/obj_labels/cards/compare_banners_fixed.png`
- 통합 검증 ROM: `build/digicomm_nyo_kr_name41.gba`
## 2026-08-28 — 사용자 확인 카드명 7개 추가 번역 (`name42`)

사용자가 원문 카드명을 확인해 준 7개를 `cards_spec.json`에 반영하고 원본
32×32 PNG에서 다시 렌더링했다.

- `E6DE68` → `대세일`
- `E6E678` → `논터보`
- `E6EE88` → `리트라이` (`리트` / `라이` 2단)
- `E6F08C` → `노카운트` (`노카` / `운트` 2단)
- `E6F89C` → `흡수` (`スイトリ` = `吸い取り`)
- `E6F494` → `슬로 룰렛` (`슬로` / `룰렛` 2단)
- `E728FC` → `흉내`

두 줄 카드명을 지원하도록 `gba_patch_obj_cards.py`에 중앙 이름 영역 클리핑을
추가했다. 글자 외곽선이 위쪽 분류 배경이나 아래쪽 COST 배경으로 침범하지 않는다.
대조 파일은 `assets/obj_labels/cards/compare_requested7.png`, 통합 검증 ROM은
`build/digicomm_nyo_kr_name42.gba`이다.

## 2026-08-28 — 글로우형 메뉴 10개 둥근 배경 복원 (`name43`)

사용자가 지적한 아래 10개 메뉴 라벨을 일본어 원본 PNG에서 다시 제작했다.

`F8D320`, `F8EB38`, `F8F340`, `F92B78`, `F94B98`, `F95BA8`, `F90350`,
`F91360`, `F92370`, `F93380`

기존 생성기는 한글 글자 주변에 얇은 외곽선만 만들었기 때문에 일본어 원본의
글자 뒤에 이어진 넓고 둥근 색면이 사라졌다. `gba_patch_obj_menu.py`의 글로우형
렌더러를 다음과 같이 고쳤다.

- 원본 항목별 팔레트에서 바탕색, 밝은 내부색, 글자색을 직접 선택한다.
- 원본 폭을 기준으로 한글 자간을 배분하되 글자 자체를 가로로 늘리지 않는다.
- 글자 뒤에 둥근 연속 배경을 만들고 바깥 테두리 → 중간색 → 밝은 내부색의
  3단 픽셀 구조를 복원한다.
- 원본 크기 128×32, indexed `P`, 투명 인덱스 0, 16색 런타임 팔레트를 유지한다.

검증 결과 10개 모두 크기·모드·투명도·팔레트가 원본과 일치하며 PNG와 ROM의
재삽입 바이트 검증도 통과했다. 확대 대조 이미지는
`assets/obj_labels/menu/compare_requested10_fixed.png`이다.

통합 검증 ROM: `build/digicomm_nyo_kr_name43.gba`
SHA-256: `347A22BD3DC46FDEAA9F04CF8ECF38AD4876651041678A1D4449A86A6213279C`

### 정정 — 수학적 원형 폐기, 실제 원본 버튼 복사 (`name44`)

`name43`의 둥근 배경은 새로 계산한 캡슐이라 원본 디자인이 아니므로 폐기한다.
원본 메뉴 19개를 다시 비교한 결과 `F8CB18`, `F91B68`, `F94390`의 원형
실루엣이 픽셀 단위로 동일함을 확인했다. 지정된 10개는 이제 새 도형을 그리지
않고 아래 실제 원본 버튼을 템플릿으로 사용한다.

- 청색 7개: `menu_F8CB18_128x32.png`의 원형 픽셀
- 보라색 3개: `menu_F94390_128x32.png`의 원형 픽셀

`menu_spec.json`의 `background_template_left_hex`가 각 항목의 원본 템플릿을
명시한다. 원형의 크기와 투명 실루엣은 모두 원본과 같은 `(6, 5, 122, 26)`이고,
그 위의 일본어만 제거한 뒤 기존 Galmuri14Bitmap 한글을 합성한다.

최종 ROM: `build/digicomm_nyo_kr_name44.gba`
`전파로 명함 교환`은 원본 원형을 좌우로 넘지 않도록 같은 글꼴을 유지한 채
`전파로 명함교환`으로 붙여 썼다. 10개 모두 최종 알파 바운딩 박스가 원형
템플릿과 동일한 `(6, 5, 122, 26)`이다.

SHA-256: `A9A98D0EEB1D563EF4575E3386CB0DDD91976EA1FABF93F54F36B9230FF362CB`

## 2026-08-28 — 남은 점장 카드 4장 판독·번역 (`name45`)

원본 32×32 카드와 Di Gi Communication 2 카드 목록/효과를 대조해 남은 네 장을
확정했다.

- `E6E270`: `ごちゃまぜ` → `뒤섞기`
- `E6F290`: `あやつり` → `조종`
- `E6FCA4`: `お手伝い・ゲマ` → `도우미` (게마 그림을 보존하기 위한 한 줄 표기)
- `E6E87C`: `店長カードX`. 별도 이름 글자를 덧쓰지 않고 기존 위쪽 `점장` 번역과
  원본의 큰 빨간 X 그림을 그대로 유지한다.

긴 한국어를 두 줄로 크게 덮으면 카드 그림이 사라지므로, 이름 영역은 원문처럼
한 줄로 제한했다. 네 PNG 모두 32×32 indexed `P`, 투명 인덱스 0, 원본 16색
팔레트 일치를 확인했고 ROM 재삽입 검증도 통과했다.

- 대조: `assets/obj_labels/cards/compare_unknown4_translated.png`
- 전체 대조: `assets/obj_labels/cards/compare_all_name45.png`
- 최종 ROM: `build/digicomm_nyo_kr_name45.gba`
- SHA-256: `B94B4613FBE01B4138D79C5519CFC37519D07C37F26EE1B58B1DD6CB6E4865DA`

## 2026-08-28 — 미니게임 ↔ 대화 전환 크래시 구조 수정

`name45`에서 미니게임 종료 후 대화로 돌아갈 때 등 화면 전환 경계에서
`E25EF004`로 분기하는 증상을 다시 조사했다. `0xE25EF004`는 ROM/RAM 주소가
아니며 ROM 본문에는 없고, GBA BIOS `0x64`/`0x13C`에 있는 ARM 명령어
`SUBS PC, LR, #4`의 기계어다. 전환 중 복귀 상태/포인터가 오염된 뒤 BIOS 코드
워드를 주소처럼 복원한 증상과 일치한다.

최근 카드/메뉴 재작화가 원인은 아니었다. `name36 -> name45` 사이 MCM 포인터 표
변경은 0개였고, `name34`부터 동일한 메시지 MCM 재배치 집합을 계속 물려받고 있었다.
다중 섹션의 압축 파이프라인도 전수 확인해 18개 모두 원본과 동일한
Huffman4(LZ77(payload)) 구조임을 확인했다.

진짜 누락은 **MCM의 물리 할당 크기 계산**이었다. 빌더는 각 압축 스트림이 실제로
소비한 바이트만 원래 슬롯으로 인정했다. 그러나 상점/미니게임의 D6/D7 스크립트는
MCM 압축 체인 뒤에 388~404바이트의 0 패딩을 의도적으로 두고, 그 뒤에 `DMS\0`
또는 `MBM\0` 컨테이너가 이어진다. 번역 후 압축 크기 증가는 28~184바이트뿐이라
이 원래 예약 패딩에 전부 들어가는데도 기존 빌더는 아래 11개를 `0xFCxxxx` 메시지
아레나로 통째로 옮기고 있었다.

`0xD60830`, `0xD624D8`, `0xD647D0`, `0xD6DBFC`, `0xD704FC`, `0xD7298C`,
`0xD74A4C`, `0xD76924`, `0xD786F4`, `0xD7A434`, `0xD7C3D4`.

이들은 상점/미니게임 시작·튜토리얼·카드 설명 등 전환 경계에 집중된 레코드다.
`gba_build.py`에 `structured_padding_after()`를 추가해 **첫 비제로 데이터가 알려진
컨테이너 태그(`DMS/MBM/MCM/DSC`)인 경우에만** 그 앞의 bounded zero padding을
원래 할당으로 인정한다. 임의의 빈 공간은 사용하지 않는다. 결과적으로 11개 모두
첫 압축 블록 포인터가 일본 원본 주소와 동일하게 유지되고, 후속 DMS/MBM 헤더까지
최소 220~368바이트 여유가 남는다. 후속 컨테이너 헤더 0x40바이트도 원본과
바이트 동일하다.

같이 발견한 감사 도구 결함도 수정했다. `cache/audit_fixed_slots.py`가 문서와 달리
첫 MCM 섹션만 읽고 있어 18개 다중 섹션을 사실상 건너뛰고 있었는데, 이제 모든
섹션 포인터를 따라 전체 체인을 합쳐 검사한다. 이름 입력의 별도 고정 슬롯
`「$0」씨…`도 내용 확인 후 의도한 payload로 제외한다.

최종 전체 체인 감사:

- 레코드 273개
- 번역 메시지 4,640개
- 슬롯 초과 0
- 번역 payload 밖 변경 0
- `0xD7298C` PASS
- 메시지 MCM 재배치 124 -> **113개**
- 구조화 패딩으로 원위치 유지 **11개 / 추가 사용 984바이트**

또 `name37~name45`에서 빌드 뒤 수동으로 얹던 카드 36장/메뉴 19장 OBJ 패치를
`build_invariants.json` + `gba_build.py` 단일 빌드 파이프라인에 통합했다. 이제
원본 ROM부터 한 번 빌드해도 최신 카드/메뉴 번역이 빠지지 않는다.

최종 구조 수정 ROM:

`build/digicomm_nyo_kr_crashfix_final.gba`

- 크기: 16,777,216 bytes
- SHA-256: `871115D671BD7EE1EE74BCDB4E51AAEC246B15DABD9193A830D9A0C24CF59EE3`

현재 세션에서는 emucap Control 인터페이스가 노출되어 있지 않아 미니게임 종료까지의
동적 플레이 재현은 수행하지 못했다. 정적 구조상 기존 전환 스크립트의 불필요한
물리 재배치는 제거됐고, 전체 해제 데이터/슬롯/후속 컨테이너 경계 검증은 통과했다.

## 2026-08-29 — 타이틀 START 방울 OBJ 한글화

타이틀 화면의 `スタート`는 로고 이미지가 아니라 ROM `0xFA686C`에서 VRAM OBJ
타일 928번으로 적재되는 별도 64×64 4bpp 스프라이트임을 동적 VRAM/OAM 대조로
확정했다. 원본은 `assets/obj_labels/title/png/title_start_FA686C_64x64.png`,
한글판은 동일 파일명의 `assets/obj_labels/title/translated_png/`에 보존한다.

방울의 고리·외곽·하단 면과 원래 보이던 주황 픽셀은 그대로 두고, 일본어가 가린
중앙 방울 부분만 원본 팔레트 9~12번으로 복원했다. `스타트`는 같은 16색 OBJ
팔레트의 흰 외곽선과 청색 명암 4단 구조로 합성했다. 결과는 64×64 RGBA이며
원본 팔레트 부분집합만 사용하고, 한정한 글자 영역 밖 픽셀 차이는 0이다.
`gba_patch_title_start.py`와 `build_invariants.json`을 `gba_build.py`에 연결해 이후
전체 빌드에서도 이 OBJ가 자동 적용된다.

## 2026-08-29 — 깨진 고정 슬롯 이미지 4장 디자인 보존 재제작

최초의 단순 원판 재제작은 원본 디자인과 다르다는 사용자 검수로 폐기했다. 최종본은
원본 PNG를 다시 기준으로 삼아 프레임·보라색 타원 실루엣·가로 결·우측 패널·하단 바를
보존하고 제목 글자만 원본의 흰색 채움, 남색 외곽선, 보라색 그림자 구조로 교체했다.
이전 결과는 `analysis/pre_requested4_remake/`에 보존했다.

- `0ad093c_240x160.png`: 요청대로 원본 팔레트의 흰색 `(248,248,248)` 단색 화면.
- `0ab43a4_240x160.png`: `이번 / 평가`, `가게 포인트`.
- `0ab5c4c_240x160.png`: 축약하지 않은 `지금까지의 / 성과`, `가게 포인트`.
- `0ae9be4_64x32.png`: 원본 상·하단 프레임과 글자 팔레트를 유지한 `당신과의`.

고정 슬롯 시험 및 최종 빌드 결과:

| 파일 | 원래 슬롯 | 새 압축 | 방식 |
|---|---:|---:|---|
| `0ab43a4` | 5,020 | 4,576 | 원본 codec 3 / 원본 tree |
| `0ab5c4c` | 5,336 | 4,728 | 원본 codec 3 / 원본 tree |
| `0ad093c` | 1,908 | 232 | 원본 codec 3 / fresh tree / unused zero |
| `0ae9be4` | 1,012 | 988 | 원본 codec 3 / fresh tree |

네 장 모두 원래 슬롯에 삽입됐고 재배치 0, 역추출 픽셀 4/4 완전 일치다.
새 통합 ROM은 `build/digicomm_nyo_kr_designmatch.gba`, SHA-256은
`836E6422EB5041620F163C4D7FC468A542EF0936D8F85DC1BA20228804377453`이다.

## 2026-08-29 — `FDC3FDC2` 재발 원인: 전환 MCM 원위치 보존 수정이 다시 유실됨

사용자가 미니게임을 한동안 진행한 뒤 튜토리얼/게임 종료에서 대화로 복귀할 때
`Jumped to invalid address: FDC3FDC2`를 다시 확인했다. 이 주소는 과거 `name11`의
MCM 코덱 오처리 때와 정확히 같아 압축 스크립트가 쓰레기로 풀린 뒤 실행되는 계열의
증상이다. 최신 ROM들의 MCM codec 필드와 실제 첫 스트림 헤더를 2,145개 전수 검사한
결과 직접적인 codec 불일치는 0건이었다.

대신 빌드 계보 비교에서 원인이 드러났다. `crashfix_final`과 `name46`은 D6/D7의
상점·미니게임 전환 MCM 11개가 모두 원래 물리 주소에 남아 있지만, 2026-08-29
01:32 생성된 `digicomm_nyo_kr_img.gba`는 아래 11개를 다시 `0xFCxxxx` 메시지
아레나로 전부 재배치했다.

`0xD60830`, `0xD624D8`, `0xD647D0`, `0xD6DBFC`, `0xD704FC`, `0xD7298C`,
`0xD74A4C`, `0xD76924`, `0xD786F4`, `0xD7A434`, `0xD7C3D4`.

즉 증상별 패치가 아니라 **빌드 파이프라인 회귀**가 근본 원인이었다. 이를 막기 위해
`build_invariants.json`의 `must_stay_in_place_records`로 11개를 선언하고,
`gba_build.py`가 해당 레코드를 재배치하려는 순간 빌드를 실패시키게 했다.

같이 고친 파이프라인 회귀:

- `--used`는 반드시 `analysis/used_sjis.json`의 정수 CP932 배열이어야 한다.
  과거 STATUS에 잘못 적힌 `hangul_map.json` 객체를 주면 이제 즉시 실패한다.
- 이미지 재삽입은 영구적으로 `in-place-only`; 이미지 MCM 재배치를 허용하지 않는다.
- 현재 111개 번역 PNG 중 `name46`과 픽셀 단위로 같은 105장은 SHA-256으로 고정한
  검증 ROM을 참조해 이미 검증된 물리 체인을 그대로 재사용하고 재삽입 후 다시
  픽셀 비교한다. 나머지 6장은 슬롯에 안 들어가므로 일본어 원본 유지.
- `人→명`, `回→회`용 8×8 MFM 글리프를 단일 빌드에 포함한다.
- `でじこ屋→데지코점`, `うさだ屋→우사다점` 고정폭 문자열을 추가했다.

새 정적 검증 빌드:

`build/digicomm_nyo_kr_stable.gba`

- SHA-256: `B9773F8465CC1321BCF042D818538FF727199ED4819014CA13E6C36C5E2EF32E`
- 16,777,216 bytes
- D6/D7 전환 MCM 재배치 **0/11**
- 전체 MCM codec/stream 불일치 **0/2,145**
- 고정 슬롯 감사: 273레코드 / 4,640메시지 / overflow 0 / 비메시지 변경 0
- 이미지 105장 삽입·105장 픽셀 역검증 / 이미지 재배치 0
- raw UI 19개 적용 (`명`, `회`, `데지코점`, `우사다점` 포함)

미니게임 종료 → 대화 복귀의 실제 플레이 검증은 이후 `stable.ss8`/`ss9`로 직접
재현했고, 위 D6/D7 재배치 방지만으로는 충분하지 않다는 것이 확인됐다.

### 2026-08-29: `E25EF004` 종료 크래시의 실제 근본 원인과 수정

사용자가 남긴 `build/digicomm_nyo_kr_stable.ss8`을 mGBA GDB 모드로 그대로
로드하면 약 10초 후 다음 실제 CPU 상태가 재현된다.

```text
PC = E25EF004
LR = 0805CA4F
SP = 03007E40
r1 = E25EF004
```

`0805CA48 ldr r1,[r1,#0x14] -> 080745D4 bx r1` 간접 호출 직전에 RAM
`0x0200101C`의 정상 콜백 구조체 포인터 `08FBD4DC`가 `00000000`으로 지워지고,
주소 0의 BIOS 명령 `04 F0 5E E2`가 구조체 필드로 오인되어 `E25EF004`를 함수
포인터로 읽는 것이 직접적인 크래시 메커니즘이다. 같은 `ss8`을 일본 원본 ROM에
로드하면 이 슬롯은 15초 후에도 `08FBD4DC`를 유지하고 크래시하지 않는다.

스테이트 8을 회귀 테스트로 사용해 패치 계층을 이분한 결과 텍스트/폰트판은 정상,
일반 MCM 이미지 105장을 넣은 순간 크래시하며, 다시 105장을 자동 이분해서 정확히
한 이미지가 원인임을 확정했다.

```text
0xB388F4  0b388f4_240x160.png  (승리 화면)
```

원본은 codec 3 / 5-section Huffman MCM인데 과거 `name46` 참조판은 이를 codec 2
LZ77로 바꿨다. 픽셀 역검증은 통과하지만 이 특정 전환 경로는 원래 codec 계약을
필요로 해서 종료 시 콜백 상태가 깨진다. 더 근본적으로 codec 3 이미지의 실제 구조는
`raw tiles -> LZ77 -> Huffman`의 **2중 압축**이다. 기존 이미지 재삽입기는 raw 타일을
Huffman에 바로 넣고 있어서 원본 4,988바이트 슬롯이 약 16KB로 폭증했고, 결국 LZ77
codec 변경으로 우회하고 있었다.

`gba_image_reinsert.py`를 원본 구조대로 고쳤다.

- codec 3: 타일 → 안쪽 LZ77 → 바깥 Huffman
- RGB가 같은 픽셀은 중복 팔레트의 원본 인덱스를 그대로 보존
- 원본 Huffman 트리 + ordinary/optimal LZ77을 먼저 시도
- 필요하면 codec 3을 유지한 채 새 Huffman 트리만 생성
- `0xB388F4`와 `0xAB9718`은 trusted-reference codec 변경을 우회하고 source codec 3 강제
- 모든 이미지 섹션은 원래 물리 할당 안에서만 재작성, 재배치 0

`0xB388F4`는 codec 3을 그대로 유지하면서 4,724/4,988바이트로 삽입됐고,
`0xAB9718`은 optimal LZ77 + 새 바깥 Huffman 트리로 정확히 432/432바이트에 들어간다.
둘 모두 삽입 후 픽셀 역검증을 통과한다.

추가 누락 UI도 같은 빌드에서 수정했다.

- `0xD6F9E8`: `よびこみカードで来い来いにょ！` → `호객 카드로 와라 와라뇨!`
- 프롤로그 전용 호객 카드 복제본 `0xE6D04C` 발견 → `업무 / 호객 / COST1`
- 이벤트 카드 `0xE6E87C`: 스펙은 고쳤지만 stale PNG가 들어가던 파이프라인 수정
- 카드 37장은 이제 매 빌드 `cards_spec.json + 일본 원본 PNG`에서 재렌더
- `終了` 화면 조각은 팔레트 `0xE6B1EC`, raw 16x16 OBJ `0xE73718`로 역추적 → `종료`
- `ＢＧ団員`, `명/회`, `데지코점/우사다점`도 단일 빌드 유지

새 검증판:

`build/digicomm_nyo_kr_stable_fixed.gba`

- 16,777,216 bytes
- SHA-256: `1DD06C85092002FA7590B68A34F797AB6CCB09494601E1A44788377F4E97708B`
- MCM 이미지: 105 verified / 0 relocated
- runtime-sensitive `0xAB9718`, `0xB388F4`: codec 3 유지 + 픽셀 역검증
- D6/D7 must-stay 11개: 첫 물리 체인 주소 원본 유지, 전체 재압축 체인 원래 할당 안에 유지
- 프롤로그/이벤트/호객 카드 ROM ↔ 생성 PNG 일치
- `0xE73718` 종료 OBJ 검증 통과
- raw UI 21개 검증 통과

동적 회귀 검증:

- `stable.ss8` + 새 ROM, 무입력 30초: `0x0200101C = 08FBD4DC`, 정상 PC, 크래시 없음
- `stable.ss9` + 새 ROM, 무입력 30초: `0x0200101C = 08FBD4DC`, 정상 PC, 크래시 없음

## 2026-08-29 — 고정 슬롯 이미지 111/111 완료

마지막 고정 슬롯 이미지들을 재배치 없이 전부 해결했다. 핵심은 그림을 무조건 더
단순화하는 것이 아니라, **화면에서 더 이상 참조되지 않는 옛 일본어 타일을 payload에
남기지 않는 것**과 **동일 RGB 중복 팔레트 인덱스를 작은 선형 글리프에서 정규화하는
것**이었다.

`gba_image_reinsert.py`에 두 개의 안전한 fallback을 추가했다.

- mapped 이미지: 일반 원본 보존 인코딩이 슬롯을 넘을 때만, 새 tilemap이 참조하지 않는
  tile slot을 0으로 정규화하고 같은 source codec으로 재압축한다. tile index와 tilemap은
  바꾸지 않으며 최종 렌더 픽셀을 다시 검증한다.
- linear 이미지: 일반 인코딩이 슬롯을 넘을 때만, 같은 RGB를 가진 중복 palette index를
  첫 index로 정규화해 재압축한다. `00fd218`처럼 40바이트밖에 없는 글리프에서 필요했다.

최종 여섯 슬롯:

| 파일 | 원래 슬롯 | 최종 압축 | 방식 |
|---|---:|---:|---|
| `00fd218_16x16.png` | 40 | 40 | codec 3, fresh tree + canonical palette |
| `0ab43a4_240x160.png` | 5,020 | 5,000 | codec 3, optimal + zero unused |
| `0ab5c4c_240x160.png` | 5,336 | 5,332 | codec 3, optimal + fresh tree |
| `0acbbac_240x160.png` | 4,128 | 4,128 | codec 3, source tree |
| `0ad093c_240x160.png` | 1,908 | 728 | codec 3, fresh tree + zero unused |
| `0ae9be4_64x32.png` | 1,012 | 888 | codec 3, fresh tree |

`0ab43a4`는 기존 번역 PNG에서 실제 번역 영역의 8×8 tile만 병합했고, 슬롯에 들어가는
최소 허용치가 31임을 탐색해 그 값을 사용했다. 이전 PNG는
`analysis/pre_final_slotfit/`에 보존했다. `0ab5c4c`, `0acbbac`, `0ae9be4`는 과거
pixel-verified probe ROM에서 다시 렌더해 `translated_png`에 승격했으며 이전본은
`analysis/pre_verified_slot_promote/`에 보존했다.

`assets/build_invariants.json`의 `verified_image_count`를 **105 → 111**로 올렸다.
실제 통합 빌드 조건(reference 103장 재사용 + source codec 강제/신규 재삽입)에서:

- MCM 이미지 **111 inserted / 111 verified**
- trusted reference 103
- failed 0
- relocated 0
- 여섯 슬롯 모두 source codec 3 유지

최종 ROM:

`build/digicomm_nyo_kr.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `E7072FBED1F7E9D06AA024A38497AE40AD98028ED0E682736BB8D7AFF9A3F06A`
- 고정 메시지 감사: 273 records / 4,640 messages / overflow 0 / non-message changes 0
- `0xD7298C` 구조 검사 PASS

동적 회귀 검증도 새 최종 ROM에서 다시 통과했다.

- `stable.ss8`, 무입력 30초: `0x0200101C = 08FBD4DC`, `E25EF004` 재현 없음
- `stable.ss9`, 무입력 30초: `0x0200101C = 08FBD4DC`, `E25EF004` 재현 없음

## 2026-08-29 — 누락 대사 회수 + 300명 목표 문구 수정

사용자 플레이 스크린샷에서 `甘くてほこほこしてとってもおいしかったわよ♡`,
`にょ～～～～っ！？`, `にょっ………………………`, `目からビー………………！`가
일본어로 남는 것을 확인했다. 원인은 번역 캐시 누락이 아니라 기존
`gba_message_stream.py`의 텍스트 판정 휴리스틱이었다. 문장 대부분이 기호인 대사와
게임 전용 하트 글리프 `0x8540`을 포함한 `03 00` 메시지가 구조적으로 정상인데도
텍스트가 아니라고 버려지고 있었다.

파서를 수정해 이 계열 메시지를 다시 회수하고, `assets/translation/recovered_messages_ko.json`
을 빌드 불변조건에 연결했다. 정확한 원문 일치와 발생 건수로 fail-closed 하며,
현재 최종 빌드에서 **회수 메시지 142건**이 추가 삽입된다. 하트는 일반 유니코드로
재인코딩하지 않고 원래 게임 코드 `0x8540`을 그대로 보존한다.

스크린샷 대사는 다음처럼 반영했다.

- `甘くてほこほこしてとってもおいしかったわよ♡` → `달고 폭신폭신해서 / 정말 맛있었어♡`
- `にょ～～～～っ！？` → `뇨～～～～！？`
- `にょっ………………………` → `뇨………………………`
- `目からビー………………！` → `눈에서 비………………！`

미니게임 목표 raw UI도 문맥에 맞게 의역했다.

- `一日で300人呼ぶ` → `300명 모으기`
- `300人` → `300명`
- `一日で呼べ！` → `먼저 모아라!`

최종 통합 검증 ROM:

`build/digicomm_nyo_kr_textfix_final.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `58263052754D04DED42AD726583EACB166262B38B5A7EC31B015D189B0BB96DF`
- `messages.json` SHA-256: `2CD689162F249231CADF80434C5AB3700DBD5CCE859C4EA41D68ECB20BDD541D`
- 기존 번역 메시지: **4,640**
- 회수 메시지: **142**
- 고정 슬롯 감사: **273 records / overflow 0 / non-message changes 0**
- `0xD7298C` 구조 검사 PASS
- MCM 이미지: **111 inserted / 111 verified / failed 0 / relocated 0**
- trusted reference: 103
- 전환 민감 MCM 11개: 첫 물리 포인터 **11/11 원본 주소 유지**
- `300명 모으기 / 300명 / 먼저 모아라!`: 최종 ROM 바이트 직접 일치 확인
- 하트 대사: 최종 payload에 `0x8540` 보존 확인
- 최종 ROM의 구조적 메시지 **4,783개 전수 역디코드**: 실제 히라가나/가타카나/한자 잔존 **0건** (`ー`, `・` 같은 의도된 일본식 기호는 잔존 판정에서 제외)

동적 회귀 테스트도 새 ROM에서 다시 통과했다.

- `stable.ss8`, 무입력 30초: `0x0200101C = 08FBD4DC`, PC `03001BE4`, 크래시 없음
- `stable.ss9`, 무입력 30초: 시작 슬롯은 0이지만 30초 후 `0x0200101C = 08FBD4DC`, PC `03001BEC`, 크래시 없음

현재 사용자가 열어 둔 mGBA가 기존 `build/digicomm_nyo_kr.gba` 파일을 잠그고 있어
표준 파일명으로의 덮어쓰기만 실패했다. 내용 검증이 완료된 최신 ROM은 위
`digicomm_nyo_kr_textfix_final.gba`이며, mGBA 종료 후 동일 빌드를 표준 파일명으로
다시 생성하면 된다.

## 2026-08-29 raw OBJ 메뉴 누락 전수 감사 + 번역 이미지 보관

실기 스크린샷에서 남아 있던 `対戦する`, `ミニゲーム`을 계기로 메뉴 raw OBJ 표를
처음부터 다시 감사했다. 기존 추출기는 `0xF91360` 앵커에서 앞/뒤 24개 64x32
조각만 훑었기 때문에, 실제 128x32 메뉴 표의 뒤쪽 절반을 놓치고 있었다.
원본 ROM을 `left + 0x404 = right`, 다음 left `+0x808` 규칙으로 다시 검사한 결과
`0xF8CB18`부터 `0xF9F440`까지 **38쌍**이 같은 메뉴 라벨 표이고, 그 다음
`0xF9FC48`부터 구조가 바뀌는 것을 확인했다. 이 중 `0xF953A0 + 0xF957A4`는
문자가 아니라 잠금용 `?` 그래픽이므로 번역 대상에서 제외했다.

기존 19개 외에 누락된 **18개**를 직접 판독해 `menu_spec.json`에 추가했다.

- `おんがく` → `음악`
- `ミニゲーム` → `미니게임`
- `保存したデータを見る` → `저장 데이터 보기`
- `食べてもまずいにょ` → `먹어도 맛없다뇨`
- `データを交換する` → `데이터 교환`
- `ふくわらい` → `후쿠와라이`
- `せつめいをよむ` → `설명 읽기` (색상 상태 2개)
- `パーティゲーム` → `파티게임`
- `ぷちゲーム` → `푸치게임`
- `ぴよこ` → `삐요코`
- `さくら＆ケムシにゅ` → `사쿠라＆케무시뉴`
- `ランキング` → `랭킹`
- `神経衰弱` → `신경쇠약`
- `ゲームをはじめる` → `게임 시작`
- `空中アクロバットよ` → `공중 아크로바틱`
- `対戦する` → `대전하기`
- `対戦開始` → `대전 시작`

메뉴는 검수용 128×32 합본뿐 아니라 실제 ROM 오프셋에 대응하는 64×32 좌/우
조각까지 모두 보관한다. 번역 대상 37개 기준으로 합본 37장 + 조각 74장이며,
원본 쪽에는 번역 대상이 아닌 잠금용 `?` 조각 2장도 별도로 남아 있다.

- `assets/obj_labels/menu/png/menu_*_128x32.png` — 원본 합본 37장
- `assets/obj_labels/menu/png/objlabel_*_64x32.png` — 원본 실제 OBJ 조각 76장
- `assets/obj_labels/menu/translated_png/menu_*_128x32.png` — 번역 합본 37장
- `assets/obj_labels/menu/translated_png/objlabel_*_64x32.png` — 번역 실제 OBJ 조각 74장

`gba_patch_obj_menu.py`가 이후 모든 빌드에서 합본과 좌/우 조각을 동시에 다시
저장한다. `gba_build.py`의 메뉴 개수 검사는 보관용 조각을 포함한 전체 `*.png`가
아니라 실제 삽입 단위인 `menu_*_128x32.png` 37장만 세도록 바꿨다.

또 ROM에는 이미 번역돼 있었지만 PNG가 남지 않던 raw OBJ도 보관 규칙에 포함했다.

- 이름 입력 버튼 6개: `assets/obj_labels/nameentry_buttons/png` /
  `assets/obj_labels/nameentry_buttons/translated_png`
- `終了 → 종료`: `assets/obj_labels/exit/png` /
  `assets/obj_labels/exit/translated_png`

`gba_patch_rom_buttons.py`, `gba_patch_exit_button.py`, `gba_build.py`,
`build_invariants.json`을 갱신해 이후 깨끗한 빌드에서도 이 원본/번역 PNG가 자동으로
생성되고 개수가 검증되게 했다. 메뉴 생성기는 기존 손검수 번역 PNG를 덮어쓰지 않고
새로 빠진 항목만 만들 수 있도록 `--only-missing-translated`도 추가했다.

최종 재빌드/역검증 ROM:

`build/digicomm_nyo_kr_imageaudit_final.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `91CCA73AB2375A001B57002FA8F40F8CE86ACFEDF200BAFED7A24171A30EFD1C`
- 고정 슬롯: **273 records / 4,640 기존 번역 / 142 회수 / overflow 0 / non-message changes 0**
- `0xD7298C` 구조 검사: **PASS**
- MCM 이미지: **111 inserted / 111 verified / failed 0 / relocated 0**
- 카드 OBJ: **37개 적용**
- 메뉴 OBJ: **37/37 translated PNG ↔ 최종 ROM 픽셀 일치**
- 이름 입력 버튼: **6/6 original/translated PNG ↔ 원본/최종 ROM 픽셀 일치**
- 종료 버튼: **1/1 original/translated PNG ↔ 원본/최종 ROM 픽셀 일치**
- 타이틀 `스타트` 적용 유지

따라서 이번 감사에서 확인된 메뉴 표 안의 일본어 번역 누락은 모두 처리됐고,
이미 번역돼 있으면서 파일 보관이 없던 raw OBJ도 재현 가능한 PNG 쌍으로 정리됐다.

## 2026-08-29 — 보드 머리 위 업무 레벨 + 미니게임 HUD 전수 수정

실기에서 캐릭터 머리 위 `販売1` 계열과 미니게임 시작/진행 HUD에 일본어가 남는
문제를 다시 원본 ROM의 raw 문자열 테이블에서 추적했다. 기존 `ui_strings` 추출기는
CP932 일본어와 ASCII `%d`가 섞인 포맷 문자열을 놓치고 있었다.

보드 머리 위 업무 레벨은 `0x143370`부터 정확히 4종 한 묶음이다.

- `よび+%d` → `호객%d`
- `整と+%d` → `정돈%d`
- `販売+%d` → `판매%d`
- `会員+%d` → `회원%d`

따라서 카드 종류별 동적 표시가 모두 한글화되며, 별도로 상태 창에서 쓰는
`0x1456F8 よびこみ`도 `호객`으로 추가했다.

같은 미니게임 HUD 블록(`0x14581C`~`0x1458DC`)도 단일 문구가 아니라 전체 조합
부품을 감사해 남아 있던 항목을 함께 고쳤다.

- `%d万円稼げ！` → `%d만원 벌어!`
- `1日に%d万円を、%d回稼げ！` → `하루 %d만원 %d회 벌어!`
- 기존 `%d日間でより多く稼げ！` 계열 유지
- 기존 `%d人のお客さんを入れろ！` 계열 유지
- `人` → `명`
- `回` → `회`
- `残り` → `남음`
- `日` → `일`

`assets/build_invariants.json`의 고정폭 raw UI 불변조건으로 넣어 원본 바이트가
다르거나 번역이 슬롯을 넘으면 빌드가 즉시 실패하도록 했다. 실제 빌드에서 모든
새 항목을 최종 ROM 바이트와 재인코딩 결과로 직접 대조해 전부 PASS했다.

사용자가 지적한 이벤트 카드 `0xE6E87C`도 이번 빌드에서 `cards_spec.json`으로 다시
렌더했고, `translated_png/objlabel_E6E87C_32x32.png`와 최종 ROM의 4bpp 타일이
픽셀 인덱스 단위로 일치하는 것을 재확인했다.

최종 ROM:

`build/digicomm_nyo_kr_uiaudit_final.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `A3B7DF4D45DA773C9E08294BBA6D6716F638C71E84231FE41F0CDB392DB7357D`
- 카드 OBJ: **37개 적용** (`E6E87C 이벤트` 재검증 PASS)
- 메뉴 OBJ: **37개 적용**
- MCM 이미지: **111 inserted / 111 verified / relocated 0**
- raw UI strings: **274개 적용**
- 고정 슬롯 감사: **273 records / 4,640 messages / recovered 142 / overflow 0 / non-message changes 0**
- `0xD7298C`: **PASS**

## 2026-08-29 — `E6E87C` 점장카드X 정정 + 실제 이벤트 카드 `E6D658` 회수

이전 감사에서 `0xE6E87C`를 이벤트 카드로 잘못 판독했다. 원본 32×32 OBJ를 다시
확대 확인한 결과 이 슬롯은 카드 전체를 가로지르는 큰 빨간 X가 있는
`店長カード×(きんし)` 계열이다. 원본의 큰 빨간 X와 일러스트는 보존하고,
텍스트만 다음처럼 다시 렌더했다.

- 상단: `店長` → `점장`
- 가운데: `店長カード` → `점장 / 카드X` 2단
- 하단: `コスト1` → `COST1`

실제 `イベント` 카드는 기존 카드 추출 시작점보다 앞에 있는 raw OBJ
`0xE6D658`이었다. 기존 `cards/png`에 없던 슬롯이라 `cards_spec.json`과
`cards/manifest.json`에 새 항목으로 추가하고 원본/번역 PNG를 모두 보관했다.

- 원본: `assets/obj_labels/cards/png/objlabel_E6D658_32x32.png`
- 번역: `assets/obj_labels/cards/translated_png/objlabel_E6D658_32x32.png`
- 번역 구성: 상단 `점장`, 가운데 `이벤트`, 하단 `COST1`

새 슬롯을 처음 추가할 때 `--source-original-pngs`가 기존 원본 PNG 존재를
가정해 실패하던 문제도 수정했다. 이제 스펙에 새 raw OBJ 슬롯이 추가됐는데
원본 PNG가 없으면 일본어 source ROM에서 그 슬롯을 자동으로 원본 PNG로 보관한
뒤 번역 렌더를 계속한다. 기존 원본 PNG는 덮어쓰지 않는다.

카드 보관/삽입 수는 **37 → 38개**로 갱신했다. 현재 `cards/png`와
`cards/translated_png` 모두 38장이고 두 신규/정정 슬롯 모두 최종 ROM에 삽입됐다.

최종 ROM: `build/digicomm_nyo_kr_cardfix_final.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `8ED5616B948CA62B7BAFFE304536EF8F44A509C5C07F1BF3C385983E0313CC09`
- 카드 OBJ: **38개 적용**
- 고정 슬롯 감사: **273 records / 4,640 messages / recovered 142 / overflow 0 / non-message changes 0**
- `0xD7298C`: **PASS**

## 2026-08-29 — 카드 raw table 46장 전수 추출 + 점장카드 X 레이어 재정정

앞선 `E6E87C=이벤트` 오판과 일부 점장카드 이름 수동 배정을 폐기했다. 카드 원본
보관은 이제 번역 대상으로 판독된 항목만 저장하지 않고 ROM `0xE6D04C`부터
`0xE72B00`까지 0x204 간격의 **46개 32x32 raw 슬롯 전부**를 매 빌드 추출한다.
`assets/obj_labels/cards/manifest.json`도 같은 46개를 자동 재생성한다.

이때 추가한 `manager_cards` 23장 순서 자동 재배정 가설은 이후 실물 PNG와 맞지 않는
것으로 확인되어 폐기했다. 메시지 정의 순서를 32x32 OBJ 슬롯 순서와 동일하다고
간주하면 과거에 직접 확인한 카드(`E6E270=뒤섞기`, `E6F290=조종`,
`E6FCA4=도우미` 등)까지 잘못 덮어쓴다. 카드 이름은 다시 `cards`의 실제
오프셋별 명시값만 사용한다.

`E6E87C`은 문자열에 X를 붙이는 방식이 아니다. 레이어 0에 `점장 / 카드`를 먼저
렌더하고, 일본 원본의 빨간 X(팔레트 index 4)를 레이어 1로 다시 복사한다. 최종
ROM에서 원본 X 픽셀 **91/91**이 한글 글자 위에 그대로 보존되는 것을 확인했다.
하단은 `COST1`이다.

최종 ROM: `build/digicomm_nyo_kr_cardtable_final.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `69018202CB880044A273372A5DBD3CF9823C0D9A03CBF36FEE67EE819D4C33D4`
- 원본 카드 PNG: **46/46** (`0xE6D04C`~`0xE72B00`)
- 번역 카드 PNG/OBJ: **40개**
- 고정 슬롯 감사: **273 records / 4,640 messages / recovered 142 / overflow 0 / non-message changes 0**
- `0xD7298C`: **PASS**

## 2026-08-29 — 카드 오프셋 육안 판독 정정 + 리트라이 1줄

사용자 실물 PNG 육안 판독을 기준으로 최근 `manager_cards` 자동 재배정을 제거하고
실제 오프셋별 스펙으로 복귀했다.

- `objlabel_E6D454_32x32` → `이벤트`
- `objlabel_E6D658_32x32` → `손패 / 체인지` 2단
- `objlabel_E6EE88_32x32` → `리트라이` 1줄

`리트라이`의 Galmuri7Bitmap 실제 글리프 폭은 30px이므로 이름 영역을
`[0,10,32,23]`으로 확장했다. 외곽선을 포함한 실제 잉크 bbox는
`(0,10)~(31,22)`로 좌우 32px를 모두 사용하며 잘림 없이 들어간다.

잘못된 자동표에서만 생성되던 `objlabel_E6D250_32x32` 번역 PNG는 보관 대상에서
제외했고, 카드 번역 PNG 불변조건을 **40 → 39**로 수정했다. 원본 raw 카드 PNG는
46장 전부 계속 보관한다.

최종 검증 ROM: `build/digicomm_nyo_kr_cardvisualfix.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `988716EE45A6AC18D9142B92BFE5D660B695CA74E9CD86090D5CC779DB84EEB9`
- 원본 카드 PNG: **46장**
- 번역 카드 PNG/OBJ: **39장**
- `E6D454`, `E6D658`, `E6EE88`: translated PNG ↔ 최종 ROM 픽셀 **일치**

## 2026-08-29 — 사용자 수정 START 이미지 고정

`assets/obj_labels/title/Gemini_Generated_Image_wjrbe2wjrbe2wjrb.png`를 START 버튼의
권위 있는 사용자 수정본으로 지정했다. 이전 빌드에서는 `gba_patch_title_start.py`가
매번 Galmuri 기반 `스타트` 이미지를 새로 그려 `translated_png`를 덮어썼지만,
이제 `build_invariants.json`의 `title_start.custom_png`를 우선 사용한다.

1024x1024 RGBA 입력의 가장자리와 연결된 흰 배경만 제거하고 실제 그림을 잘라,
원본 START OBJ의 가시 영역 `(8,14)-(54,50)` = 46x36 px에 맞춘 뒤 런타임 4bpp
16색 팔레트로 양자화한다. 따라서 이후 전체 빌드에서도 사용자 수정본이 다시
자동 생성본으로 바뀌지 않는다.

최종 ROM: `build/digicomm_nyo_kr_startcustom.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `2006E6FFE8A95CE948C0A2C6082185A81352300FBA1505770613CA2BA110223E`
- START 가시 픽셀: **1,029**
- START bbox: **(8,14)-(54,50)** (원본과 동일)
- `translated_png/title_start_FA686C_64x64.png` ↔ 최종 ROM `0xFA686C`: **완전 일치**

## 2026-08-29 — 동적 상점명 `ゲーマーズ%s店` + 블랙 게이머즈 누락 수정

실기 상점 화면에서 점장 이름을 끼워 넣는 `ゲーマーズ%s店`이 일본어로 남는 문제를
원본 ROM에서 직접 추적했다. 기존 raw UI 추출기는 CP932 문자열 안에 ASCII `%s`가
섞인 이 포맷 문자열을 놓치고 있었다.

추가한 raw UI 번역:

- `0xAB1508` `ゲーマーズ` → `게이머즈`
- `0xAB1514` `ブラックゲーマーズ` → `블랙게이머즈`
- `0xAB23B4` `ゲーマーズ%s店` → `게이머즈%s점`

마지막 항목의 `%s`는 점장 이름이 들어가는 런타임 자리표시자이므로 그대로 보존했다.
사용자가 기억한 `ブラックゲマゲマ団店` 문자열은 ROM에 존재하지 않았고, 실제 블랙
상점명 리소스는 `ブラックゲーマーズ`였다.

전체 빌드 후 세 오프셋 모두 `encode_text()` 결과와 최종 ROM 바이트가 완전히
일치하는 것을 검증했다.

최종 ROM: `build/digicomm_nyo_kr_shopnamefix.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `B99A943ADB00B728251F42EAD06F3EA5FF00DF63BB18990DA722E164A7C4DEFA`
- raw UI strings: **277개 적용**
- MCM 이미지: **111 verified / relocated 0**
- 카드 OBJ: **39개**
- 메뉴 OBJ: **37개**
- START 사용자 수정본 유지

## 2026-08-29 — 사용자 `0b36c60` 이미지 표시 시 E3A02004 크래시 수정

사용자가 직접 만든 `Gemini_Generated_Image_el034kel034kel032.png`를 240×160으로
변환해 `0b36c60_240x160.png`에 적용한 첫 빌드는 정적 픽셀 검증은 통과했지만,
해당 화면 표시 시 `Jumped to invalid address: E3A02004`로 종료됐다.

원인은 전체 MCM 할당 5,932바이트만 맞추고 개별 압축 섹션 경계를 다시 패킹한 것이다.
문제판은 섹션 시작이 `0xB36C98 / 0xB37454 / 0xB37EC0`으로 바뀌었다. 일본 원본의
실제 시작 주소는 `0xB36C98 / 0xB374D0 / 0xB37EDC`, 슬롯은 각각
`2,104 / 2,572 / 1,256`바이트다. 이 게임은 MCM 포인터 테이블만 따르지 않고 압축
블록의 실제 소비 길이를 따라 물리적으로 연속 해제하므로, 총합이 들어가더라도 중간
섹션 경계가 움직이면 런타임에서 압축 데이터를 코드/포인터로 오인할 수 있다.

최종 수정 방식:

- **디자인 소스는 사용자 새 PNG** `Gemini_Generated_Image_el034kel034kel032.png`를 사용한다.
- 새 PNG를 240×160 / 원본 64색 팔레트 계약에 맞추고, 원본의 165타일 한도에 들어가도록
  필요한 타일만 최소 통합한다. 일본 원본 그림으로 되돌려 합성하는 방식은 사용하지 않는다.
- raw payload의 codec 3, chunk 4096, tile count 165를 유지한다.
- codec 3의 원래 구조인 **raw tiles -> LZ77 -> Huffman** 중첩을 그대로 유지한다.
- 새 PNG용 LZ77을 만들 때 일반 LZ 파서가 원본 Huffman tree에 없는 바이트를 만들 수
  있으므로, `gba_lz77_encode.compress_huffman_compatible()`를 추가했다. 이 파서는
  **원본 Huffman tree에 실제 존재하는 byte만 사용하면서** 동등한 LZ77 스트림을 만든다.
- 세 Huffman tree는 재생성하지 않고 **일본 원본 tree table 바이트를 그대로 재사용**한다.
- 압축 결과가 짧으면 Huffman tree 자체를 늘리지 않는다. 대신 LZ77이 자기 raw-size를
  채운 뒤 무시하는 trailing byte를 inner stream에 넣어 바깥 Huffman block의 실제 소비
  길이를 원본 슬롯에 정확히 맞춘다.
- `assets/build_invariants.json`에서 `0xb36c60`의 섹션 시작 보존을 영구 강제한다.

최종 `0b36c60`:

- section destinations: `0xB36C98 / 0xB374D0 / 0xB37EDC`
- section sizes: **`2,104 / 2,572 / 1,256` — 원본과 완전 동일**
- Huffman tree 3개: **원본과 byte-for-byte 동일**
- codec 3 / chunk 4096 / tile count 165 유지
- 픽셀 역검증 PASS
- 재배치 0
- 인코딩 보고서: `source_tree_exact_sections_zero_unused`

전체 수정 ROM: `build/digicomm_nyo_kr_customlogo_originalmcm.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `594F5790416196B95FDA133714B158E373BC42D0B2CF26FA14C069E389AFBD0A`
- MCM 이미지: **111/111 verified**, failed 0, relocated 0
- 카드 OBJ 39개 / 메뉴 OBJ 37개 / raw UI 277개 / 사용자 START 유지

부팅 프레임도 일본 원본·교체 전 정상판·새 빌드를 동일하게 스캔했다. 사용자가 캡처한
무지개 형태와 같은 전환 프레임은 **일본 원본 자체에서도 존재한다**. 예를 들어 390f에서
일본 원본은 4,484색, 기존 정상 한글판과 새 빌드는 4,859색으로 순간적으로 색이 섞이고,
그 전후 프레임은 다시 정상 팔레트 화면으로 돌아온다. 따라서 이 한 프레임의 색 섞임은
새 PNG 포맷 파손이 아니라 원래 게임의 화면 전환 효과다. 별개로 새 `0xb36c60`의 ROM
저장 방식은 위 조건대로 일본 원본과 동일한 tree/section 구조로 교체했다.

### 절대 불변조건 — MCM 압축 섹션 경계

**앞으로 이 프로젝트의 MCM 이미지는 압축 섹션 경계/시작 주소를 절대 움직이지 않는다.**
총 압축 크기가 원래 전체 할당 안에 들어간다는 이유로 앞 섹션의 남는 공간을 뒤 섹션이
당겨 쓰게 만들면 안 된다. 각 섹션은 반드시 **원본의 시작 주소와 자기 물리 슬롯 범위**를
그대로 유지해야 한다.

- MCM section pointer/start address: **원본과 동일하게 고정**
- 각 압축 block의 물리적 끝이 다음 원본 section start를 침범하면: **빌드 실패 처리**
- 전체 chain 합계만 보고 재패킹/재정렬: **금지**
- 섹션 사이를 0 패딩으로 메우는 방식: **금지** — 게임이 소비 길이로 다음 블록을 찾는다.
- 공간이 부족하면: 이미지 단순화·타일 병합·압축 최적화 등으로 **해당 섹션 자체를 자기
  원래 슬롯 안에 맞춘다.** 그래도 안 들어가면 해당 번역 이미지를 넣지 않는다.
- 새 MCM 이미지 작업 때는 픽셀 역검증뿐 아니라 **모든 section start가 원본과 동일한지
  반드시 검증**한다.

이번 `0xb36c60`의 `E3A02004` 크래시를 이 규칙의 근거 사례로 유지한다. 이 규칙은
`0xb36c60` 한 장만의 예외가 아니라 **향후 MCM 수정 전체에 적용하는 작업 원칙**이다.

## 2026-08-29 — 사용자 재교체 `0b36c60_240x160.png` 적용

사용자가 `translated_png/0b36c60_240x160.png`를 다시 직접 교체했다. 이번 파일은
240×160 RGBA PNG이며 SHA-256은
`A876100DC813DE698267D82FC8E8816F8C3DFBF67DAB7542F4D418FCE58E0D30`이다.
이 파일 자체를 권위 있는 소스로 두고 **파일은 덮어쓰지 않는다.** 빌드 시에만 원본
64색 GBA 팔레트와 165타일 제한으로 변환한다.

원본 MCM 계약은 그대로 유지한다.

- codec 3 / chunk 4096 / tile count 165
- section starts: `0xB36C98 / 0xB374D0 / 0xB37EDC`
- section sizes: `2,104 / 2,572 / 1,256`
- 총 압축 크기: `5,932 / 5,932`
- 원본 Huffman tree 재사용
- relocated 0 / 픽셀 역검증 PASS

사용자 PNG를 원본 팔레트로 스냅한 뒤 생기는 추가 고유 타일 3개만 기존 타일로
통합한다. 마지막 섹션은 시각 오차가 가장 작은 실측 조합 `130 / 134 / 150` 타일을
기존 타일로 재사용해 1,256바이트 슬롯 안에 맞춘다. 이 변환은 ROM 인코딩 시 메모리
안에서만 수행하고 사용자가 넣은 PNG 원본은 유지한다.

최종 빌드: `build/digicomm_nyo_kr_user_b36_fixed.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `7BE8213B58419CD76DAE07F47334AFB46A50CA982C4F46AF9E048D4F26BC6B6F`
- MCM 이미지 **111/111 verified**, failed 0, relocated 0
- Mesen 부팅 450프레임 검사에서 invalid-address 이벤트 없음

## 2026-08-30 — 사용자 RGB 8-bit/channel 재저장본 적용 확인

`translated_png/0b36c60_240x160.png`를 사용자가 RGB 모드로 다시 저장했다.
현재 파일은 **RGB 240×160**이며 PNG SHA-256은
`99972B269249C36527AF8ED9A2CB6BC9B7F7F0331FF239B2DEA407B6E09FD27D`이다.
파일에는 7,596개의 고유 RGB 색이 있으므로 GBA 원본 64색 팔레트로의 런타임
인코딩 변환은 여전히 필요하다. 사용자 PNG 자체는 빌드 중 수정하거나 덮어쓰지 않는다.

직전 RGBA 파일(`A876100D...`)과 현재 RGB 파일의 **디코드된 RGB 픽셀 바이트는 완전히
동일**하며 픽셀 SHA-256은 둘 다
`FBDB532B8CCFD08F3FAFE64538AE2BF65DF73419E3EB79893C1ED8BE72DE5E08`이다.
즉 이번 변경은 PNG 저장 형식(RGBA → RGB)만 바뀐 것이고 화면 픽셀 자체는 바뀌지 않았다.
따라서 GBA 64색/165타일 정규화 결과도 동일하며 최종 ROM 역시 기존
`digicomm_nyo_kr_user_b36_fixed.gba`와 바이트 단위로 동일하다.

재빌드 결과: `build/digicomm_nyo_kr_rgb8_b36.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `7BE8213B58419CD76DAE07F47334AFB46A50CA982C4F46AF9E048D4F26BC6B6F`
- `0xB36C60`: codec 3 / chunk 4096 / tile count 165 유지
- section starts: `0xB36C98 / 0xB374D0 / 0xB37EDC`
- section sizes: `2104 / 2572 / 1256` — 원본과 동일
- MCM 이미지 **111/111 verified**, failed 0, relocated 0
- 카드 OBJ 39개 / 메뉴 OBJ 37개 / 사용자 START 이미지 유지

## 2026-08-30 — `0b36c60_240x16022355.png` 런타임 정상화 + 미니게임 HUD 전용 글리프 패치

위의 이전 `0xB36C60` 기록은 당시 잘못된 정적 파서 전제를 포함한다. 사용자가 실제
mGBA에서 컬러 노이즈/invalid-address를 재현한 뒤 원본 구조를 다시 역추적해 다음이
최종적으로 확인됐다.

### `0xB36C60` 최종 런타임 계약

사용자 입력은 `assets/image_extraction/japanese_images/0b36c60_240x16022355.png`.
240×160 P 모드이며 실제 사용 색 51개가 원본 64색의 정확한 부분집합이다.

정상 원본의 실제 계약:

- raw tile payload: **10,688B / 167 tiles**
- section starts: **`0xB36C98 / 0xB374E4 / 0xB37F14`**
- physical rooms: **`2,124 / 2,608 / 1,136B`**
- encode 방향: **raw tiles → RLE(0x30) → Huffman8(0x28)**
- 세 Huffman tree는 **원본 바이트를 그대로 유지**

새 PNG는 168개 고유 타일이므로 1개만 가장 가까운 타일로 접어 167개로 맞췄다.
시각 차이는 `(55,87)`의 **1픽셀**뿐이다. 이후 타일의 64/64/39 섹션 배치를 tilemap과
함께 무손실 재배치해 원본 tree로 각 슬롯에 맞췄다. 최종 사용량은
`2,080 / 2,608 / 1,136B`이며 첫 섹션은 RLE 종료 뒤 무시되는 데이터만 추가해 물리
경계를 정확히 복원했다. 이 방식의 ROM은 사용자가 mGBA에서 실제 정상 표시를 확인했다.

정상 기준 ROM:
`build/digicomm_nyo_kr_b36_22355_exacttree_test.gba`

### 미니게임 전용 HUD 글리프

스크린샷에서 남아 있던 `2日目`, `あと300万円`, `あと300人`은 일반 메시지 폰트가
아니라 ROM `0xD7CBC4`의 **448×32 / 28글자 전용 글리프 시트**에서 조립된다.
하드코딩된 글리프 번호는 다음처럼 확인됐다.

- `10+11 = あと` → **`앞으로`** (32px 전체 폭에 3글자를 그려 두 셀로 분할)
- `12 = 円` → **`엔`**
- `13 = 人` → **`명`**
- `14 = 日` → **`일`**
- `15 = 目` → **`째`**
- `16 = 万` → **`만`**

`0xD7CBC4`는 220 tiles / 14,080B, codec 3이며 실제 중첩은
**raw → LZ77(0x10) → Huffman4(0x24)**다. 원본 섹션 시작과 물리 길이를 모두 유지했다.

- starts: `0xD7CC00 / 0xD7D1E0 / 0xD7D980 / 0xD7E08C`
- rooms: **`1,504 / 1,952 / 1,804 / 924B`**
- Huffman4 tree 4개: **원본 byte-for-byte 유지**
- 정적 픽셀 역검증: PASS

캐릭터 머리 위 상태 문자열도 최종 ROM에서 다시 검증했다.

- `호객%d` @ `0x143370`
- `정돈%d` @ `0x143378`
- `판매%d` @ `0x143380`
- `회원%d` @ `0x143388`

이 경로가 사용하는 별도 10×10 MFM 폰트에도 `호/객/정/돈/판/매/회/원`의 stand-in
CP932 셀이 모두 한글 비트맵으로 들어 있는 것을 직접 검사했다.

상점명 표기는 요청에 따라 소스 `translation/ui_strings_ko.json`도 함께 변경했다.

- `ブラックゲーマーズ` → **`블랙 게이머즈`**
- `ブラックゲーマーズ前` → **`블랙 게이머즈 앞`**

최종 테스트 ROM:
`build/digicomm_nyo_kr_minigame_text_test.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `DC7D5F62C33E0EC34CEEADAD8F591D042669F10E8BBB88B5F44A9D419819E878`
- 직전 HUD 글리프판과의 차이: **21 bytes**, 정확히 두 상점명 슬롯만 변경
- `0xB36C60` 영역: 직전 정상판과 byte-for-byte 동일
- `0xD7CBC4` 영역: 직전 HUD 글리프판과 byte-for-byte 동일
- mGBA state8 10초 회귀: invalid-address 없음, `0x0200101C = 08FBD4DC` 유지

재현 도구:
- `tools/gba_patch_b36_exacttree_test.py`
- `tools/gba_patch_minigame_hud_glyphs.py`
- `tools/gba_finalize_minigame_text_test.py`

## 2026-08-30 — 사용자 `messages.json` 최신 수정 반영

사용자가 수정한 `assets/translation/messages.json`을 다시 빌드했다. 고정 슬롯 검사에서
`0xD13FF4:0xFC`가 50바이트 슬롯에 52바이트를 요구해 빌드가 즉시 중단되었고,
`말썽쟁이가 날뛰며 굿즈 효과를 무효화하는 점장 카드`를 같은 의미의
`말썽쟁이가 날뛰어 굿즈 효과를 없애는 점장 카드`로 축약해 슬롯 계약을 맞췄다.

일반 MCM 재삽입에서 `0xB36C60`은 기존처럼 exact-tree 특수 경로가 필요하므로 일반
재압축을 강행하지 않았다. 나머지 이미지 110장은 in-place 검증 후, 카드 39장·메뉴
37장·사용자 START·종료 버튼을 재적용하고 `gba_patch_minigame_photoshopfix.py`로
`0xB36C60`/HUD/상태 라벨을 기존 fix7 정상 계약과 byte-for-byte 동일하게 복구했다.

최종 ROM: `build/digicomm_nyo_kr_message_updated.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `959FB1E88E9CB4FBABF0BF2010330773182BBD31B753967547E7818BE13DEA3F`
- `messages.json` SHA-256: `ED017E7726C3C5491AC5616B86156C21740DC095FAEAB912119A0D007F1FDE65`
- 고정 슬롯 감사: **273 records / 4,640 messages / recovered 142 / overflow 0 / non-message changes 0**
- `0xD7298C`: **PASS**
- 일반 MCM 이미지: **110 verified / failed 0 / relocated 0** (`0xB36C60`은 특수 exact-tree 경로)
- `0xB36C60`, HUD, 상태 MFM, 날짜 OBJ: 기존 `photoshopfix7`과 byte-for-byte 동일
- minigame recovery whitelist 밖 변경: **0 bytes**
- emucap 600프레임 회귀: 크래시/이벤트 없이 완료

## 2026-08-30 — `万円/人` 재회귀 원인 제거 및 단일 빌드 체인 고정

`messages.json` 최신 반영 ROM에서 사용자가 미니게임 목표 화면의 `300万円` 재등장을
확인했다. 바이너리 감사 결과 `build/digicomm_nyo_kr_message_updated.gba`의
`0xD00000~0xD80000`에 숫자+`万円` **85곳**, 숫자+`人` **9곳**이 다시 존재했고,
직전 정상 `photoshopfix7.gba`에는 둘 다 0곳이었다.

원인은 `gba_patch_minigame_textfix_final.py`가 정식 `gba_build.py` 밖의 수동 후처리로
남아 있었던 것이다. 메시지만 다시 빌드하면 이 94개 동일길이 단위 치환이 빠지고,
후속 `photoshopfix7` 복구는 그래픽/폰트/상태 라벨만 소유하므로 raw script 단위를
고치지 못했다.

재발 방지 수정:

- `tools/gba_build.py` 마지막 단계에 `gba_patch_minigame_textfix_final.py`를 영구 통합
- 숫자+`万円` 85곳 → 숫자+`만엔`, 숫자+`人` 9곳 → 숫자+`명` 자동 치환
- 그 뒤 `gba_patch_minigame_photoshopfix.py`를 CLI 입력 ROM 방식으로 실행해
  B36/HUD/기본 상태/카드효과 상태/날짜 라벨의 승인된 runtime 계약을 자동 복구
- 일반 MCM 재삽입에서는 exact-tree 전용 `0xB36C60`을 제외하고 **110장만** 검증;
  B36은 recovery 단계에서 167타일/원래 section pointer/전체 physical span으로 복구
- 최종 recovery 뒤 숫자+`万円` 또는 숫자+`人`이 하나라도 남으면 `gba_build.py`가 실패

원본 ROM에서 수동 중간파일 없이 클린 전체 빌드를 수행해 통과했다.

최종 ROM: `build/digicomm_nyo_kr_message_updated2.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `D0064FBE13082B9C94B01185E23355700979914361A456DADDC9210F9798FC57`
- 숫자+`万円`: **0건**
- 숫자+`人`: **0건**
- 단위 치환: **94건** (`万円` 85 + `人` 9)
- 고정 슬롯 감사: **273 / 4,640 / recovered 142 / overflow 0 / non-message 0**
- `0xD7298C`: PASS
- 일반 MCM: **110 verified / failed 0 / relocated 0**
- B36 / HUD / STATUS_MFM / DATE_OBJ: 기존 정상 `photoshopfix7`과 byte-for-byte 동일
- emucap 600프레임 회귀: PASS

## 2026-08-30 — `0ad093c` 흰 화면 원인 확정 + 실제 런타임 수정

사용자가 백업 메모리 초기화 화면이 흰 화면으로만 보인다고 보고해 `0ad093c_240x160`을 정적 역추출이 아니라 Mesen frame-step으로 다시 검증했다. 기존 빌드는 PNG 역검증과 섹션 크기 검사는 통과했지만 실제 frame 300/320에서 화면 대부분이 깨졌다.

직접 원인은 codec 3 해석 오류였다. 이 자산의 원본 두 Huffman 섹션을 한 단계만 해제하면 각각 4096 / 3136바이트의 raw palette-index tile 데이터가 바로 나온다. 첫 바이트도 LZ77 헤더 `0x10`이 아니라 배경 palette index `0x11`이며, Huffman alphabet은 원본 17색 index 1..17이다. 즉 `0ad093c`는 일반 MCM의 `raw -> LZ77 -> Huffman` 구조가 아니라 **`raw tiles -> Huffman8` 직결 구조**다. 기존 패처가 중간 LZ77을 잘못 추가했기 때문에 recursive 정적 디코더에서는 그림이 맞아도 게임에서는 LZ 스트림을 타일로 읽었다.

수정:
- 최종 PNG를 `assets/image_extraction/japanese_images/translated_png/0ad093c_240x160.png`에 실제 보관하고 이 파일을 유일한 빌드 입력으로 고정. `analysis` source override 제거.
- 원본 600-word tilemap byte-for-byte 유지.
- tile count 113 유지.
- 섹션 시작 `0xAD0970 / 0xAD0D58`, 물리 슬롯 `1000 / 908B` 유지.
- 이 오프셋에만 `direct Huffman8` 불변조건을 적용하고 LZ77 단계를 금지. 다른 codec-3 자산은 기존 nested 경로를 유지한다.
- `build_invariants.json`, `gba_build.py`, `gba_image_reinsert.py`, 이미지 작업 `TRANSLATION_NOTES.md`에 계약을 영구 등록했다.

Mesen clean boot frame 검증:
- frame 300: 2색, 배경 38,096px + 빨간 한글 304px, bbox `(79,51)-(159,104)`
- frame 320: 동일
- frame 300/320 pixel-for-pixel 동일
- 이전 흰 화면/대량 깨짐 재현 없음

검증 ROM: `build/digicomm_nyo_kr_0ad093c_runtimefix2.gba`
SHA-256: `4A659E5EDEB76866F235E1B40C70D21B4DC77DDDDDBC1615AF628FDB57CEFF52`

## 2026-08-30 — `0ad093c_240x160` 한글 초기화 화면 클린 빌드 재확인

`0ad093c_240x160.png`는 백업 메모리 초기화 안내 화면이다. 과거 슬롯 적합 시험 중
흰 단색 임시본과 `analysis/lowentropy_candidates4` override를 사용한 적이 있었지만,
현재 최종 빌드 정책은 `assets/image_extraction/japanese_images/translated_png/0ad093c_240x160.png`를
유일한 권위본으로 사용한다. `assets/build_invariants.json`에는 이 자산의 direct Huffman,
원본 tilemap, section start/room 계약만 고정되어 있고 `source_overrides`는 사용하지 않는다.

현재 후보:

- 표시 문구: `백업 메모리를 / 초기화하고 있습니다`
- 240×160, 2색 `(248,248,248)` / `(248,32,32)`
- SHA-256: `A5242BD28AB9925390D37E6AF03387AA4AC7FCFCDCC322FD3ABE7654DB98EB16`

원본 ROM과 현재 `messages.json`에서 새 출력으로 클린 전체 빌드를 다시 수행했다.

새 확인 ROM: `build/digicomm_nyo_kr_0ad093c_fixed.gba`

- 크기: **16,777,216 bytes**
- SHA-256: `D0064FBE13082B9C94B01185E23355700979914361A456DADDC9210F9798FC57`
- 직전 최신 `digicomm_nyo_kr_message_updated2.gba`와 **byte-for-byte 동일**
- `0ad093c`: codec 3 / tile count 113 / in-place
- section starts: `0xAD0970 / 0xAD0D58` — 원본과 동일
- section rooms/sizes: `1000 / 908` — exact
- encoding: `fresh_tree_exact_sections_zero_unused`
- 픽셀 역검증: **PASS**
- relocated: **0**

즉 이 초기화 화면 번역은 수동 중간파일이 아니라 현재 정식 클린 빌드 체인에 포함되어
있고, 이후 `messages.json`을 다시 수정해 빌드해도 같은 한글 화면이 유지된다.

## 2026-08-30 — `0ad093c` 한글 글리프 가독성 재작업 + ROM 직접 반영

사용자 실기 피드백에서 기존 한글 획이 깨져 보이는 문제가 확인됐다. 원인은 슬롯 적합용
후보가 `Galmuri11Bitmap`의 16px strike를 12px로 최근접 축소해 사용하면서 한글 획이
끊기고 겹친 것이었다. 새 자산은 `Galmuri7.ttf`를 7px 네이티브 픽셀 크기로 직접 렌더한다.

원본 tilemap은 blank tile 0을 여러 셀에서 재사용하므로, 새 글자를 임의 위치에 그리면
fixed tilemap conflict가 난다. 첫 줄은 row 7의 재사용 셀을 단어 사이 공백으로 이용해
`백업 / 메모리를`를 분리 배치했고, 둘째 줄 `초기화하고 있습니다`는 row 12의 연속 unique
타일 구간에 배치했다.

최종 PNG:
- `assets/image_extraction/japanese_images/translated_png/0ad093c_240x160.png`
- 240×160 / 2색
- 빨간 픽셀 247px, bbox `(87,57)-(158,102)`
- SHA-256 `4396C728BD122F824E9FF1B571F4C619F2F94EA77DC0CE0B933EC8A74F79B15E`

최종 직접 반영 ROM:
- `build/digicomm_nyo_kr_0ad093c_readable_final.gba`
- SHA-256 `88590AC39F6E8C0C18E85C3C3BBB470AFFD8EF029A7A6C711B96DA1AAB4F1F4A`
- 기준 ROM `digicomm_nyo_kr_0ad093c_final2.gba` 대비 변경 235 bytes 전부 `0xAD0970..0xAD10E3` 내부
- codec 3 / direct Huffman / tile count 113 / tilemap byte-for-byte preserved
- section starts `0xAD0970 / 0xAD0D58`
- section rooms/sizes `1000 / 908` exact
- verified 1 / failed 0 / relocated 0

재발 방지를 위해 `cache/finalize_three_slots.py`도 예전 `lowentropy_candidates4` PNG를 다시
복사하지 않고 `cache/remake_0ad093c_readable.py`로 이 가독성 자산을 재생성하도록 변경했다.

## 2026-08-30 — 이름 입력 받침 2단계 조합 + 포커스 확대 최종 반영

기존 한글 이름 입력은 완성형 음절 1키=1글자 방식이라 받침 없는 음절만 제공했다. 이번에는
원본 이름 버퍼 포맷을 바꾸지 않고 입력 핸들러 `0x080500C4`를 확장했다.

입력 방식:
- 먼저 받침 없는 음절을 입력한다. 예: `기`, `바`, `저`.
- `전환` 페이지의 받침 키를 선택하면 새 글자를 추가하지 않고 직전 음절을 교체한다.
  예: `기 + ㅁ -> 김`, `바 + ㄱ -> 박`, `저 + ㅇ -> 정`.
- 이름 버퍼는 기존과 동일한 16바이트 / CP932 stand-in 2바이트×최대 8글자 구조를 유지한다.
- 뒤로 가기는 기존 동작 그대로 완성된 음절 하나를 통째로 삭제한다.

구현:
- 새 패처: `tools/gba_nameentry_batchim.py`
- `gba_build.py` 이름 입력 단계에 `gba_nameentry_page2.py` 다음 순서로 영구 연결.
- 2페이지의 좌/우 상단 27셀에 종성 `ㄱㄲㄳㄴㄵㄶㄷㄹㄺㄻㄼㄽㄾㄿㅀㅁㅂㅄㅅㅆㅇㅈㅊㅋㅌㅍㅎ`를 배치.
- 토큰 코드는 `0xEA80..0xEA9A`; source text와 2,350자 한글 map이 사용하지 않는 메인 12x12 폰트 슬롯 27개를 재사용한다.
- 같은 코드를 이름 입력 전용 8x8 MFM에도 추가해 일반 셀과 포커스 확대가 같은 받침을 참조한다.
- 포커스 확대용 12x12 글리프는 승인된 8x8 받침 픽셀을 크게 재도트한 전용 비트맵이며, 메인 폰트 전체 이전은 하지 않는다.
- 받침 토큰 자체는 이름 버퍼에 저장되지 않고, 성공한 경우 직전 2바이트 완성형 음절 코드만 교체된다.
- 입력 함수는 8바이트 Thumb trampoline으로 `0x08FDF800`의 새 핸들러에 연결한다.
- canonical built Hangul map에서 `(받침 없는 음절, 종성) -> 완성형 음절` 1,996쌍을 생성해
  `0xFDFA80` 이후에 저장한다. 조합 결과는 전부 이름칸 12x12 메인 폰트에 존재함을 빌드 시 검증한다.

사용자가 이름 입력 화면에 저장한 최신 `state1`로 mGBA GDB 런타임 검증:
- 실제 name-entry object: `0x02007090`
- 실제 이름 버퍼: `0x02006178`
- 일반 입력 경로: `나`가 정상 추가됨.
- 초기 실험판에서 페이지 변환 후 종성 토큰 `ㄲ` 선택 시 이름 길이는 1글자 그대로 유지되고 `나 -> 낚`으로 교체됨 — PASS.
- 최종 구현은 토큰 코드를 `0xEA80..0xEA9A`로 옮겨 포커스 12x12 글리프와 공유한다.
- 사용자가 실제 이름 입력 화면에서 **받침 포커스 확대 표시가 정상 동작함을 확인**했다.
- 8글자/16바이트 이름 버퍼 계약, 기존 뒤로가기, 기존 이름 렌더 함수는 그대로 유지한다.

최종 ROM:
`build/digicomm_nyo_kr_final.gba`
SHA-256: `DE6CCA049CA9AF54C610B3382059C0D1E48D5FFBCE7E2AFABC808BFD820FEF50`
크기: `16,777,216 bytes`

기준 `digicomm_nyo_kr_cards_messages_exit_final.gba` 대비 변경은 9,092바이트이며,
허용 범위(입력 trampoline, 받침용 12x12 슬롯 27개, 8x8 MFM count/글리프,
page2 변환표, 새 Thumb 핸들러/포인터/조합표) 밖 변경은 **0바이트**다.

## 2026-08-31 — 미니게임 2판 이후 시작 화면 일본어 복제본 수정

미니게임 시작 화면의 `데지코점 / 우사다점 / 목표 문구`가 1판만 한글이고 2판부터
일본어로 돌아가는 원인을 원본 ROM에서 추적했다. 이 시작 데이터는 공용 문자열 하나가
아니라 각 판 스크립트에 복제돼 있으며, 기존 raw UI 테이블은 첫 판 주소만 번역하고 있었다.

원본에서 `でじこ屋`와 `うさだ屋`는 각각 7개 스테이지 블록에 존재한다. 2~7판의
누락 주소를 `assets/translation/ui_strings_ko.json`에 추가했고, 인원 목표 2·3판과
금액 목표 4~7판의 시작 문구도 같은 방식으로 전부 추가했다.

- 2판: `0xD717F4 / 0xD71B84`, `300명 모으기 / 300명 / 먼저 모아라!`
- 3판: `0xD738E8 / 0xD73C78`, `300명 모으기 / 300명 / 먼저 모아라!`
- 4판: `0xD757B4 / 0xD75B4C`, `하루 50만엔 벌기 / 50만엔 / 하루 벌어라!`
- 5판: `0xD7758C / 0xD77920`, `하루 150만엔 벌기 / 150만엔 / 하루 벌어라!`
- 6판: `0xD79298 / 0xD7962C`, `하루 150만엔 벌기 / 150만엔 / 하루 벌어라!`
- 7판: `0xD7B32C / 0xD7B6E4`, `500만엔 벌기 / 500만엔 / 벌어라!`

원본 ROM부터 클린 전체 빌드 후 새로 추가한 30개 슬롯을 재인코딩 바이트와 직접 대조해
30/30 PASS했다. 미니게임 시작 데이터 범위에서 `でじこ屋`, `うさだ屋`,
`一日で呼べ！`, `一日でかせげ！`, `500万円かせぐ` 잔존은 모두 0건이다.

최종 ROM:
`build/digicomm_nyo_kr_final.gba`
SHA-256: `D41B6D9CD2B0DCC2279B90988C6496A0CCCD6DCD950EDC60CBC1EA6797D2CCAD`
크기: `16,777,216 bytes`

## 2026-08-31 — 친구와 놀기 / 대전하기 검은 화면 수정

사용자가 메인 메뉴 `친구와 놀기 -> 대전하기` 직전 저장한 `state3`에서 A를 눌러 실제
mGBA 런타임을 재현했다. 당시 최종 ROM은 약 2초 뒤 화면의 **93.97%가 순수 검정**으로
바뀐 뒤 진행하지 않았고, 일본 원본 ROM은 같은 상태/입력에서 정상 대전 화면으로 넘어갔다.

직접 원인은 표시 직전에 읽는 `0xACA74C`의 456x32 MCM이었다. 일본 원본은:

- codec 3
- 직접 Huffman4 (`0x24`), LZ77 단계 없음
- 섹션 시작 `0xACA780 / 0xACADD0`
- 섹션 물리 길이 `1616 / 364 bytes`

인데 기존 통합 빌드는 정적 픽셀 검증만 통과한 trusted reference를 복사하면서 codec을
**2(LZ77)** 로 바꾸고 두 번째 섹션도 `0xACABF8`로 앞당겼다. 이 자산만 일본 원본
체인으로 복구한 `aca74c_original_restore_probe.gba`는 같은 state3+A에서 정상 진입했다.

번역 PNG를 유지한 채 direct Huffman8 exact-slot, fresh Huffman4 exact-slot + 타일 3개
재배치까지 시험했으나 둘 다 정적 픽셀 역검증은 PASS하면서 실제 런타임에서는 흰/회색
화면으로 고정됐다. 따라서 `0xACA74C`는 현재 번역 재삽입 대상에서 제외하고 일본 원본
리소스를 byte-for-byte 유지한다. `translated_png/0aca74c_456x32.png`는 연구용으로만 남긴다.

후속 조사에서 `0xD152CC`의 `ほっけみりん` 번역까지 원인으로 본 것은 잘못된 판단이었다.
사용자가 `홋케미린`을 의도적으로 확정한 정상 문구라고 확인했으므로 `messages.json`의
header `0/20/40/80` 네 곳은 모두 **`홋케미린`으로 유지**한다. 앞서 추가했던
`임연수 구이` 강제 불변조건과 `gba_build.py`의 전용 검사는 제거했다.

실제로 검은 화면이 재현된 ROM과 일본 원본의 구조적 차이로 확정된 것은 `0xACA74C`이다.
이 자산은 번역 PNG가 존재하더라도 자동 재삽입하지 않고 원본 MCM 체인을 그대로 보존한다.

재발 방지:

- `build_invariants.json`: `0xACA74C`를 `generic_reinsert_excluded_offsets`에 등록.
- `runtime_exact_assets[0xaca74c]`에 direct-Huffman4/섹션 주소/슬롯 계약 기록.
- `messages.json`: `0xD152CC` 네 슬롯은 사용자 확정 번역 `홋케미린` 유지.
- `runtime_fixed_message_translations`와 `임연수 구이` 전용 빌드 검사는 사용하지 않는다.

현재 클린 빌드에서 `0xACA74C..0xACB303`은 일본 원본과 byte-for-byte 동일하고,
MCM section pointer `0xACA780 / 0xACADD0`, 물리 길이 `1616 / 364`, block head
`0x24 / 0x24`도 원본과 동일하다. `0xD152CC` 첫 항목을 최종 Hangul map으로 역디코드하면
`홋케미린`임을 확인했다.

최종 ROM:
`build/digicomm_nyo_kr_final.gba`
SHA-256: `16CB2BB5BFF04E216A9DEE2F75F3220BA727526D657993814325555316207A1A`
크기: `16,777,216 bytes`
