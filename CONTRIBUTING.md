# 기여 가이드

이 저장소는 **도구 코드, 추출된 텍스트/메타데이터, 번역 작업 데이터**를 공개합니다. ROM·게임 바이너리·그래픽/음원 같은 실제 게임 자산이 섞이지 않도록 하는 것이 가장 중요합니다.

## 번역 수정

번역을 새로 하거나 기존 번역을 수정하기 전에 **반드시 `TRANSLATION_GUIDE.md`를 먼저 읽어 주세요.** 이 프로젝트는 AI 번역 기반 초벌을 포함하므로 기존 한국어 문장을 정답으로 가정하면 안 됩니다. `messages.json`의 일본어 원문과 가능하면 실제 게임 장면을 함께 확인한 뒤 수정하세요.

AI를 이용해 재번역할 경우에도 기존 한국어 번역만 입력하지 말고 일본어 원문, 화자/장면 정보, 용어집을 같이 제공하는 것을 권장합니다.

### 메인 메시지

`assets/translation/messages.json`의 `translation` 필드만 수정하는 것을 기본으로 합니다.

- `mcm_offset`, `mcm_offset_hex`, `header`는 위치 식별자입니다.
- `$0`, `$1`, `$2`, `$3`, `%d` 등 제어/치환 토큰을 삭제하거나 번역하지 마세요.
- `\n`은 화면 줄바꿈이므로 의도 없이 변경하지 마세요.
- 전각/반각 문자 차이가 실제 바이트 길이와 렌더링에 영향을 줄 수 있습니다.
- 기존 용어는 `GLOSSARY.md`, `glossary.tsv`를 우선 따릅니다.

### 고정 UI / 복구 메시지

- `assets/translation/ui_strings_ko.json`
- `assets/translation/recovered_messages_ko.json`

위 파일의 번역 값을 수정합니다. 원문 키/오프셋은 매칭에 사용되므로 구조 변경은 별도 근거가 있을 때만 해주세요.

## 도구 수정

Python 도구를 수정했다면 최소한 아래 검사를 통과시켜 주세요.

```bash
python -m compileall -q tools
```

JSON을 수정했다면 파싱 오류가 없는지 확인합니다.

가능하면 수정 이유, 대상 오프셋, 재현 절차, 검증 결과를 PR 설명이나 `STATUS.md`에 남겨 주세요.

## 절대 커밋하지 말아야 하는 파일

다음 파일은 저장소에 올리지 않습니다.

- 원본/수정 ROM (`*.gba`, `*.rom` 등)
- 게임에서 추출한 바이너리 (`*.bin`, raw 덤프 파일)
- BIOS
- 세이브/세이브스테이트
- 원본 또는 번역 이미지 (`*.png`, `*.jpg`, `*.psd`, `*.xcf`, `*.svg` 등)
- 폰트 파일 (`*.ttf`, `*.otf` 등)
- IPS/BPS/xdelta 등 패치 바이너리
- 빌드 결과물 및 에뮬레이터 캡처

텍스트/JSON/TSV 형태로 추출한 원문, 오프셋, 매니페스트, 구조 분석 자료는 후속 개발용으로 커밋해도 됩니다. 실수로 실제 게임 바이너리/이미지를 추가했다면 커밋 전에 반드시 제거하세요.

## 커밋 전 확인

```bash
git status --short
git diff --check
python -m compileall -q tools
```

그리고 staged 파일 목록에 ROM·게임 바이너리·이미지·폰트·패치 바이너리가 없는지 직접 확인합니다. 추출 텍스트/JSON 메타데이터는 허용됩니다.

## 라이선스

기여하기 전에 `LICENSES.md`를 확인해 주세요.

도구 코드 기여는 MIT License로 배포되는 것에 동의하는 것으로 간주합니다. 한국어 번역 기여는 비영리 팬 번역/연구/보존 목적의 공유·수정 허용 범위로 프로젝트에 제공하는 것으로 간주합니다.

게임 원문, 캐릭터, 그래픽, 상표 등 제3자 권리를 기여자가 새로 라이선스할 수 있는 것은 아닙니다.
